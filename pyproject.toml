[project]
name = "jared"
version = "0.31.0"
description = "Jared — GitHub Projects v2 board steward (Claude Code plugin)"
requires-python = ">=3.11"
dependencies = [
    "pyyaml>=6.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "ruff>=0.4",
    "mypy>=1.10",
]

# This project is a Claude Code plugin, not a Python distributable. We use
# pyproject.toml only for dev-tool config (pytest/ruff/mypy) and to pin
# deps for the venv. Tell setuptools not to try to build a wheel.
[tool.setuptools]
packages = []

[tool.ruff]
line-length = 100
target-version = "py311"
# The CLI entry point is extension-less, so ruff's directory walk (*.py/*.pyi)
# skips it. Without this, `ruff check .` / `ruff format .` silently ignore the
# most-invoked file in the repo.
extend-include = ["skills/jared/scripts/jared"]

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "B", "SIM"]

[tool.mypy]
strict = true
python_version = "3.11"
files = ["skills/jared/scripts", "skills/jared/scripts/jared", "tests"]
exclude = ["skills/jared/scripts/__pycache__"]
explicit_package_bases = true
mypy_path = "."

[tool.pytest.ini_options]
markers = [
    "integration: requires TESTBED_* env + network; opt-in via -m integration",
]
testpaths = ["tests"]
pythonpath = ["."]  # Allow `from skills.jared.scripts.lib.board import ...`
addopts = "-m 'not integration'"
