# Marketplace-Readiness Review — Coverage Inventory & Findings Ledger

**Epic:** #348 · **Spec:** `docs/superpowers/specs/2026-06-10-marketplace-readiness-review-design.md`
**Status:** living document — updated each phase.
**Severity:** **P0** = ship-blocker · **P1** = must-fix before release · **P2** = polish (optional, else deferred to this writeup).

This is the durable artifact the spec promises. The **coverage inventory** is the anti-silent-skip
checklist: every surface Phases 1–2 must touch, with a `covered` marker set when it is reviewed/walked.
The **findings ledger** accumulates every defect with evidence and disposition.

---

## Coverage inventory

**Marker key:** ☑ reviewed this phase · ◑ partially covered · ☐ not yet covered.

Phase 1 reviewed **code and prose**. Phase 2 still owes the **live walkthrough** of every slash command
and the runtime verification of each CLI invariant — a ☑ below is not a substitute for that.

**How to read the findings column.** Every F-id in these tables was generated by matching each finding's
recorded `location` field against the surface path — not written by hand. A surface may be cited *as
evidence* rather than *as the defect site*: `CHANGELOG.md` appears against F14 because F14 quotes it as a
contradicting source, and `README.md` appears against F14/F15 for the same reason, while README's own
defect is **F12**. Read the finding to see which role the citation plays. **Bold = P1.**

*(An earlier draft of this section carried hand-written F-numbers. They were wrong — F3 is the KanbanFlow
number-index defect, not README's — and were replaced with programmatically-derived citations. Noted here
because mis-resolving a citation is one of the defect classes this very review hunts.)*

### Slash commands (9) — *Phase 2 live walkthrough still owed*

| # | command | 1c static review | Phase-2 live walk |
|---|---|---|---|
| 1 | `/jared` | ☑ | ☐ |
| 2 | `/jared-init` | ☑ | ☐ |
| 3 | `/jared-file` | ☑ | ☐ |
| 4 | `/jared-start` | ☑ | ◑ (used live to start #349 and #350) |
| 5 | `/jared-stage` | ☑ | ☐ |
| 6 | `/jared-groom` | ☑ | ☐ |
| 7 | `/jared-audit` | ☑ | ☐ |
| 8 | `/jared-reshape` | ☑ | ☐ |
| 9 | `/jared-wrap` | ☑ | ☐ |

### CLI subcommands (19) — *Phase 2, each verifies the invariant it owns*

All 19 loaded cleanly in the Stage-0 `--help` cold probe (no MCP, no auth). Phase-1a read the handlers and
Phase-1e mapped them to tests; runtime invariant verification is Phase 2's.

| # | subcommand | invariant to verify | Phase-2 runtime |
|---|---|---|---|
| 1 | `file` | atomic: issue created **and** on board **and** Status+Priority set | ◑ (filed #348–#354) |
| 2 | `move` | Status transition to a valid column | ◑ (moved #349, #350) |
| 3 | `set` | single-select field set by name | ☐ |
| 4 | `close` | close + verify/force auto-move to Done | ☐ |
| 5 | `comment` | comment posted to issue | ☐ |
| 6 | `blocked-by` | native GitHub dependency edge add/`--remove` | ◑ (wired 6 edges) |
| 7 | `get-item` | JSON: number + item-id + field values | ◑ |
| 8 | `summary` | one-screen board status | ◑ |
| 9 | `add-to-board` | idempotent recovery path | ☐ |
| 10 | `next-session-prompt` | board-derived handoff skeleton | ◑ |
| 11 | `ties` | board-level ties for target issue | ◑ |
| 12 | `audit` | audit working-set fetch | ☐ |
| 13 | `session-resolve` | resolve action; nonzero on REFUSE_* | ◑ (PROCEED_SOLO) |
| 14 | `session-lock-write` | write presence lock | ◑ |
| 15 | `session-lock-clear` | remove presence lock | ☐ |
| 16 | `worktree-add` | worktree + fresh branch from origin/main | ☐ |
| 17 | `wrap-state` | wrap-time state computation | ☐ |
| 18 | `propose-partition` | partition proposal across issues | ☐ |
| 19 | `migrate` | cross-backend copy; surface losses; on-success doc flip | ☐ (riskiest — both directions) |

### Library modules (15) — *Phase 1a correctness / 1b security*

| module | Phase-1 findings |
|---|---|
| `board.py` | **F5** (P1), **F7** (P1), **F13** (P1), F20, F21, F22, F31, F50 |
| `board_provider.py` | F24 |
| `github_provider.py` | **F5** (P1), **F13** (P1), F22, F23, F49 |
| `kanbanflow_provider.py` | **F3** (P1), **F5** (P1), **F13** (P1), F24 |
| `kanbanflow_client.py` | **F5** (P1), F18 |
| `kf_number_index.py` | **F3** (P1) |
| `cache.py` | clean — no finding |
| `capabilities.py` | F57 |
| `migrate.py` | **F4** (P1), **F5** (P1), F18 |
| `partition.py` | clean — no finding |
| `ties.py` | clean — no finding |
| `session_lock.py` | clean — no finding |
| `worktree.py` | clean — no finding |
| `wrap_state.py` | clean — no finding |
| `__init__.py` | clean — no finding |

### Batch scripts (6) — *Phase 1a*

| script | Phase-1 findings |
|---|---|
| `sweep.py` | **F13** (P1), **F14** (P1), **F35** (P1) |
| `bootstrap-project.py` | **F14** (P1) |
| `dependency-graph.py` | F51 |
| `capture-context.py` | **F34** (P1), F43, F44, F45, F49 |
| `archive-plan.py` | clean — no finding |
| `stage.py` | **F6** (P1), **F13** (P1), F25 |

### Reference docs (16) — *Phase 1c doc↔code drift*

| doc | Phase-1 findings |
|---|---|
| `operations.md` | F27 |
| `jared-cli.md` | **F11** (P1) |
| `dependencies.md` | **F10** (P1), **F37** (P1) |
| `plan-spec-integration.md` | F28 |
| `pii-pre-flight.md` | F55 |
| `board-sweep.md` | **F35** (P1), **F36** (P1), F51, F52 |
| `context-capture.md` | F52 |
| `design-rationale.md` | clean — no finding |
| `human-readable-board.md` | **F36** (P1), **F37** (P1) |
| `migration.md` | **F38** (P1), **F39** (P1) |
| `milestones-and-roadmap.md` | F53 |
| `new-board.md` | **F39** (P1), **F40** (P1), F53, F54 |
| `parallel-sessions.md` | **F41** (P1) |
| `session-continuity.md` | **F42** (P1), F56 |
| `structural-review.md` | F57 |
| `voice.md` | F58, F59 |

### Assets / templates (4) — *Phase 1c*

| template | Phase-1 findings |
|---|---|
| `issue-body.md.template` | **F37** (P1) |
| `session-note.md.template` | clean — no finding |
| `project-board.md.template` | **F9** (P1) |
| `plan-conventions.md.template` | clean — no finding |

### Top-level docs (7) — *Phase 1c / 1f onboarding*

| doc | Phase-1 findings |
|---|---|
| `README.md` | **F12** (P1), **F14** (P1), **F15** (P1) |
| `docs/getting-started.md` | **F14** (P1), **F15** (P1), F33 |
| `docs/bake-sites.md` | F47 |
| `docs/github-api-tool-selection.md` | F48, F49, F50 |
| `CHANGELOG.md` | **F14** (P1) |
| `CLAUDE.md` | **F36** (P1) |
| `docs/project-board.md` | F25 |

### Slash-command stubs (9) — *Phase 1c static review; Phase 2 owes the live walk*

| stub | Phase-1 findings |
|---|---|
| `jared.md` | clean — no finding |
| `jared-init.md` | clean — no finding |
| `jared-file.md` | F25, F33 |
| `jared-start.md` | **F14** (P1) |
| `jared-stage.md` | F26 |
| `jared-groom.md` | F58 |
| `jared-audit.md` | clean — no finding |
| `jared-reshape.md` | clean — no finding |
| `jared-wrap.md` | clean — no finding |

`skills/jared/SKILL.md` — **F8** (P1), **F14** (P1), F27, F58, F59

### Entry point & build config (2) — *added in Phase 1; the original inventory omitted both*

The coverage inventory listed 15 library modules and 6 batch scripts but never listed the CLI entry point
or the build config as review surfaces — so five findings had no row to hang on. Added here. The omission
is thematically apt: `skills/jared/scripts/jared` is the same extension-less file that sat outside the
ruff/mypy directory walks until `dc8dbb0`, and it is the repo's most-invoked file.

| surface | Phase-1 findings |
|---|---|
| `skills/jared/scripts/jared` (CLI entry point, argparse wiring + all `_cmd_*` handlers) | **F7** (P1), F16, F17, F19, F46 |
| `pyproject.toml` (deps, `requires-python`, ruff/mypy config) | **F13** (P1), F29 |

### Plugin metadata (2) — *Phase 1d packaging* — ☑ clean
`.claude-plugin/plugin.json` · `.claude-plugin/marketplace.json` — both parse; required keys present;
version parity `0.30.0` confirmed against `pyproject.toml` by Stage 0. **No metadata defect found.**

### Tests (61 files / 21,297 LOC) — *Phase 1e quality audit* — ☑ with a methodology caveat
Reviewed by `test:fakes-vs-contract` and `test:coverage-gaps` (F30, F31, F32). **Caveat recorded by the
completeness critic:** filename-based coverage inference is unreliable here — CLI-layer tests are
consolidated in `tests/test_cli.py:169–401` rather than per-command files, so a future 1e finder must grep
`main([` call sites, not directory listings. The critic caught one of its own first-pass inferences failing
exactly this way, which is why the caveat is recorded rather than the inference.

### Residual gaps — declared, not hidden

Phase 1's exit criterion is "no silent skips." These are the **non-silent** ones:

- **`CHANGELOG.md`, `CLAUDE.md` and `docs/project-board.md` were never reviewed as primary targets** —
  they appear in the tables above only because other findings cite them as evidence. Round 2 found real
  drift in 11 of 11 reference docs that round 1 had left unexamined, so these three should not be presumed
  clean. Recommended as the first target of any Phase-1 round 3.
- **Phase-2 runtime verification is owed** for 13 of 19 CLI subcommands and 8 of 9 slash commands.
- **`migrate` remains the riskiest unverified surface** — three findings (F4, F5, F18) landed against it
  from static review alone, and no direction of the cross-backend copy has been exercised live.

---

## Phase 0 — baseline & clean room  *(recorded 2026-06-11)*

### Offline baseline — dev venv (system Python 3.14)
| check | result |
|---|---|
| `ruff check .` | **PASS** — all checks passed |
| `ruff format --check .` | **PASS** — 83 files already formatted |
| `mypy` (`--strict` per pyproject) | **PASS** — no issues in 83 source files |
| `pytest` (unit) | **PASS** — 900 passed, 3 deselected, 9.5s |

This is the **regression floor**: any red introduced in Phase 3 is ours.

> **Baseline re-verified 2026-09-11 (commit `10ddbd7`), at the start of Phase 1.** The table above
> carries the *corrected* counts. The 2026-06-11 recording read 82 files / 890 tests / 59 test files;
> two PRs merged in the interim moved it to **83 files / 900 tests / 61 test files**, still all-green.
> The regression floor for Phase 3 is the corrected row, not the original.
>
> **Two findings were fixed out-of-band between Phase 0 and Phase 1 — finders must not re-file them:**
> - `dc8dbb0` — the extension-less CLI entry point `skills/jared/scripts/jared` was skipped by ruff's
>   and mypy's directory walks; all gates reported clean while the file carried 9 ruff + 7 mypy errors.
>   Fixed via `[tool.ruff] extend-include` + an explicit path in `[tool.mypy] files` (82 → 83 files).
> - `a787145` — `skills/jared/SKILL.md` frontmatter was not valid YAML.
>
> **One Phase-0 gotcha is now stale:** the note that board ops need `env -u GH_TOKEN` because the
> VM's PAT lacks `project` scope no longer holds on this machine — the active token carries `project`
> and every board op in this session succeeded with it set.

### Live integration baseline — both backends
| backend | test | result |
|---|---|---|
| GitHub Projects v2 (`jared-testbed`) | `test_integration_item_add_idempotency` | **PASS** |
| KanbanFlow (live board `p9vK6cR`) | `test_kanbanflow_live.py` (2 tests) | **PASS** (run with `KANBANFLOW_API_TOKEN`, `GH_TOKEN` unset) |

### Clean room
- **Host:** `docker.lan` — SSH key-auth OK, Docker 29.5.2. (`proxmox.lan` LXC fallback unavailable: key not authorized — **not needed**, docker.lan is sufficient.)
- **Clean-room job #1 — stdlib-only cold run** (fresh `python:<v>-slim`, **no `pip install`**, repo mounted read-only, `py_compile` + `compileall` + `jared --help`):

| 3.9 | 3.10 | 3.11 | 3.12 | 3.13 | 3.14 |
|---|---|---|---|---|---|
| COLD-OK | COLD-OK | COLD-OK | COLD-OK | COLD-OK | COLD-OK |

→ **Version floor ≤ 3.9** (no 3.10+ syntax; `match` would fail `py_compile` on 3.9). **PyYAML provably unnecessary** — the CLI imports and builds its parser with zero third-party packages. The cold-install story is structurally sound.

---

## Findings ledger

| id | sev | dim | finding | evidence | status |
|---|---|---|---|---|---|
| **F1** | P2 | 1d | `pyyaml>=6.0` is declared a runtime dependency in `pyproject.toml`/`requires.txt` but is **never imported** anywhere in shipped code or tests (only a comment references `seed-issues.yaml`). Dead metadata; remove from `[project] dependencies`. | repo-wide `import yaml` grep empty; cold-run COLD-OK with no pip install | **confirmed** (re-confirmed Phase 1 as F27 — still live at `pyproject.toml:7`) → fix in Phase 3 |
| **F2** | P2 | 1c | The phase issues filed on the board use a bold **Deliverables** block, not the `## Acceptance criteria` / `## Depends on` headers that `/jared-start`'s pullable check keys on. Likely a convention-doc clarification (epic-child phase issues vs. leaf work items) rather than a code defect — but worth a deliberate ruling. | observed starting #349; reproduced again starting #350 (2026-09-11); body has no `## Acceptance criteria` header | **adjudicated in Phase 1c → F19**: convention-doc clarification, *not* a code defect. Epic-child phase issues are exempt by design (`stage.py:184-197` `is_epic`); the convention doc should say so explicitly. |

### Positive confirmations (closed, non-issues)
- **Cleanliness gate passes** — `.gitignore` thorough; `tests/testbed.env` has zero git history (secret never committed); only `testbed.env.example` tracked. (Full-history secret scan still owed in Phase 1b.)
- **CLI is pure stdlib** — cold-runs on Python 3.9–3.14 with no dependencies.
- **No orphan session lock** — the `.jared/session-346.lock` seen earlier was already cleared by its wrap; `session-resolve` returned `PROCEED_SOLO`.

---

## Phase 1 — findings  *(recorded 2026-09-11)*

**Result: 57 confirmed findings — 0 P0, 22 P1, 35 P2.**

Two rounds. Round 1 ran 13 finders across dims 1a–1f (80 agents); its completeness critic ruled the
"no silent skips" exit criterion **not met** — 12 of 16 reference docs and the 281-line
`capture-context.py` had drawn zero citations, and a two-item spot-check of that cohort found two real
defects. Round 2 (59 agents) read exactly those surfaces and found **23 more**, confirming the critic:
the zero-finding status reflected absence of review, not absence of defects.

Every finding below was adversarially verified — skeptics prompted to *refute*, defaulting to refuted on
weak evidence, with perspective-diverse lenses (correctness / reachability / evidence for 1a–1b;
accuracy / intent / impact for 1c–1f). A finding survives only if fewer than a majority refute it.
Six findings were severity-corrected **downward** by their verifiers; those are marked. The one finding
that drew a refuting vote is flagged as a weak survivor rather than presented as equally certain.

**What the verification bar actually was — read this before trusting a severity.** The plan specified
"keep a finding iff fewer than a majority refute it," and that rule was implemented as written. But with
**two** verifiers per non-P0 finding, a majority is two — so a single refuting vote never killed anything,
and the effective bar for 56 of the 57 findings was *"at least one verifier declined to refute."* No
finding in this set drew a P0's third verifier, because none was rated P0. **F17 is therefore not an
exception; it is the only case where the rule was actually exercised** — one of its two verifiers refuted
it and it survived on the tie. Phase 3 should treat the P2 tier in particular as "one reviewer confirmed,
one reviewer looked" rather than as consensus. Raising the non-P0 verifier count to three would make the
majority rule bite; that is a change to the plan, not a defect in this run.

**Agent spend: 139 total (80 + 59), against a 55–115 estimate.** The overrun is real and is recorded here
rather than smoothed over: round 2's five finders returned 24 findings where ~10 were projected, and each
finding pulls two verifiers.

### ⚠ Correction to the Phase-0 baseline — "Python floor ≤ 3.9" was a false positive

Phase 0 recorded **"Version floor ≤ 3.9"** from a clean-room matrix of `py_compile` + `compileall` +
`jared --help` across 3.9–3.14, all COLD-OK. **That conclusion is wrong**, and Phase 1's 1d finder
refuted it (see F13): `datetime.UTC` is 3.11-only and is used in `stage.py:20`, `board.py` (7 call sites),
`sweep.py` (6 call sites), `github_provider.py:645`, `kanbanflow_provider.py:322` and `jared:1484`.

The probe proved those files **parse** on 3.9; the claim it was used to support was that they **run** on 3.9.
Those diverge exactly at runtime attribute lookups, which `py_compile` never evaluates. `--help` didn't
catch it either, because the parser builds without touching those code paths.

**`requires-python = ">=3.11"` is therefore correct** and needs no change. What needs correcting is the
ledger's own Phase-0 claim — done here — and the Phase-0 methodology lesson: a compile probe answers a
syntactic question and cannot substantiate a runtime one.

### Findings index

| id | sev | dim | finding | location |
|---|---|---|---|---|
| **F3** | P1 | 1a | kf_number_index reseed silently collapses duplicate task numbers with no detection, contradicting its own… | `skills/jared/scripts/lib/kanbanflow_provider.py:233-243,391-393` |
| **F4** | P1 | 1a | migrate.py's cross-ref rewriter lacks ties.py's false-positive guard, corrupting text on kanbanflow->github | `skills/jared/scripts/lib/migrate.py:101` |
| **F5** | P1 | 1a | blocked-by edges to a closed/Done endpoint are silently dropped, not converted -- and undisclosed at all on… | `skills/jared/scripts/lib/migrate.py:54-78` |
| **F6** | P1 | 1a | stage.py computes "today" from local system clock, not UTC, mismatching GitHub's UTC timestamps | `skills/jared/scripts/stage.py:521` |
| **F7** | P1 | 1b | jared wrap-state's gh pr view call skips the GH_TOKEN/GITHUB_TOKEN scrub, and a failure silently reads as "no PR" | `skills/jared/scripts/jared:1585-1606` |
| **F8** | P1 | 1c | SKILL.md's 'jared close polls for auto-move' claim contradicts tested implementation (no poll) | `skills/jared/SKILL.md:116, :204, :206` |
| **F9** | P1 | 1c | session-handoff-prompt config documents a /jared-wrap file-write feature that has no consumer and is contradicted… | `skills/jared/assets/project-board.md.template:125` |
| **F10** | P1 | 1c | dependencies.md misdescribes dependency-graph.py's KanbanFlow fallback as reading label emulation; it actually… | `skills/jared/references/dependencies.md:7` |
| **F11** | P1 | 1c | Backend-gate boxes across reference docs quote fabricated `degraded: ...` strings that never appear in the code | `skills/jared/references/jared-cli.md:114,254` |
| **F12** | P1 | 1d | README Prerequisites never mentions Python or git, both hard runtime dependencies of every slash command | `README.md:44-62` |
| **F13** | P1 | 1d | Stage-0's "proven Python floor ≤3.9" is a false positive — datetime.UTC (3.11+-only) breaks /jared-stage and… | `skills/jared/scripts/stage.py:20` |
| **F14** | P1 | 1f | getting-started.md states the wrong WIP cap default (3 vs. the actual 4) | `docs/getting-started.md:107` |
| **F15** | P1 | 1f | getting-started.md tells a newcomer to type bare `jared` CLI commands into their shell, but nothing in the… | `docs/getting-started.md:30-32, 135, 161` |
| **F34** | P1 | 1a | append_decision uses substring containment, not equality, for its idempotency check — silently drops distinct… | `skills/jared/scripts/capture-context.py:150-171` |
| **F35** | P1 | 1c | board-sweep.md's Up Next overstock threshold (>3) doesn't match sweep.py's actual cap (8), which has no override… | `skills/jared/references/board-sweep.md:45` |
| **F36** | P1 | 1c | human-readable-board.md's Labels section documents a `blocked` label as valid, contradicting the project's own… | `skills/jared/references/human-readable-board.md:112` |
| **F37** | P1 | 1c | human-readable-board.md's issue-body skeleton shows a `## Depends on` / `## Blocks` shape the real template… | `skills/jared/references/human-readable-board.md:39-69,81` |
| **F38** | P1 | 1c | migration.md documents a `--diff` flag on bootstrap-project.py that does not exist | `skills/jared/references/migration.md:11` |
| **F39** | P1 | 1c | Reference docs give bare `scripts/*.py` invocations that fail from a real project checkout | `skills/jared/references/migration.md:11,55,92` |
| **F40** | P1 | 1c | new-board.md's blank-project Status list omits Blocked | `skills/jared/references/new-board.md:35` |
| **F41** | P1 | 1c | parallel-sessions.md's per-worktree venv setup omits the dev extras needed to run tests | `skills/jared/references/parallel-sessions.md:102-103` |
| **F42** | P1 | 1c | session-continuity.md's orientation example fabricates sections next-session-prompt never emits, and contradicts… | `skills/jared/references/session-continuity.md:129-150` |
| **F16** | P2 | 1a | wrap-state conflates any `gh pr view` failure (auth/network/rate-limit) with "no PR exists" | `skills/jared/scripts/jared:1599-1606` |
| **F17** | P2 | 1a | PR check-status classification is fail-open: any unrecognized check state reports as "passed" | `skills/jared/scripts/jared:1619-1628` |
| **F18** | P2 | 1a | Dry-run KanbanFlow call estimate hardcodes comment_count=0, undermining the one number meant to catch a… | `skills/jared/scripts/jared:2040-2049` |
| **F19** | P2 | 1a+1c | jared CLI's top-level --help usage line omits two real subcommands (audit, propose-partition) that command stubs… | `skills/jared/scripts/jared:69-87` |
| **F20** | P2 | 1a | Git-remote repo fallback uses wrong directory for root-level PROJECT_BOARD.md | `skills/jared/scripts/lib/board.py:133` |
| **F21** | P2 | 1a | Repo: bullet accepted without owner/name validation, crashes downstream with a raw ValueError instead of… | `skills/jared/scripts/lib/board.py:174` |
| **F22** | P2 | 1a | fieldValues(first:20) / projectItems(first:10) have no truncation guard, unlike the outer issues connection — can… | `skills/jared/scripts/lib/github_provider.py:191-239` |
| **F23** | P2 | 1a | Transient-read retry (#342) covers migrate's reads but not get_item()/_find_item_id(), which query the identical… | `skills/jared/scripts/lib/github_provider.py:382-405` |
| **F24** | P2 | 1a | KanbanFlow comment author falls back to a raw internal user-id, violating the documented "" contract | `skills/jared/scripts/lib/kanbanflow_provider.py:352-357 and :359-374` |
| **F25** | P2 | 1c | F2 adjudication: phase-tracking issues using bold **Deliverables** instead of ## Acceptance criteria —… | `commands/jared-file.md:32-38` |
| **F26** | P2 | 1c | commands/jared-stage.md documents a stale Up Next cap default (3, should be 8) | `commands/jared-stage.md:108` |
| **F27** | P2 | 1c | Hand-written 'degraded:' examples in SKILL.md/operations.md don't follow the phrasing they claim is canonical | `skills/jared/SKILL.md:204,230,291,293,299,310` |
| **F28** | P2 | 1c | plan-spec-integration.md claims archive-plan.py commits its changes; the script has no git-commit code at all | `skills/jared/references/plan-spec-integration.md:127` |
| **F29** | P2 | 1d | pyyaml>=6.0 is a dead runtime dependency — confirmed, scope is a one-line removal | `pyproject.toml:7` |
| **F30** | P2 | 1e | wrap-state's PR-status JSON→enum translation has zero test coverage | `skills/jared/scripts/jared:1619-1628` |
| **F31** | P2 | 1e | `jared propose-partition` silently drops the NATIVE_DEPENDENCIES degradation note on KanbanFlow — and no test… | `skills/jared/scripts/jared:1664` |
| **F32** | P2 | 1e | propose-partition's existing-session-label parsing (CLI-only logic) is never exercised | `skills/jared/scripts/jared:1665-1674` |
| **F33** | P2 | 1f | getting-started.md's description of the `/jared-file` interactive flow omits the mandatory milestone question | `docs/getting-started.md:44-46` |
| **F43** | P2 | 1a | --show silently discards --current-state / --decision when passed together, exiting 0 with no indication the… | `skills/jared/scripts/capture-context.py:227-234` |
| **F44** | P2 | 1a | Module docstring's 'Preserves all other sections exactly' is false — reassemble silently reorders custom sections… | `skills/jared/scripts/capture-context.py:4` |
| **F45** | P2 | 1a | split_sections silently drops content when a body has two identically-named H2 headings | `skills/jared/scripts/capture-context.py:99-129` |
| **F46** | P2 | 1a | _cmd_close prints two different "OK:" success lines for one close operation | `skills/jared/scripts/jared:544 and :555` |
| **F47** | P2 | 1c | docs/bake-sites.md cites a non-existent asset filename (missing .template suffix) | `docs/bake-sites.md:29` |
| **F48** | P2 | 1c | Stated '~10 graphql points per project item' rate contradicts the doc's own replicated primary measurement | `docs/github-api-tool-selection.md:11` |
| **F49** | P2 | 1c | Doc still describes the issue-body REST migration as proposed/pending though it shipped | `docs/github-api-tool-selection.md:15,80,185,187` |
| **F50** | P2 | 1c | 'Open questions' MCP-scope-diagnostic item is stale — the doc's own 'What shipped' section says it's done | `docs/github-api-tool-selection.md:225` |
| **F51** | P2 | 1c | board-sweep.md's example dependency-graph.py invocation omits the required --repo flag, so it fails as written | `skills/jared/references/board-sweep.md:102` |
| **F52** | P2 | 1c | context-capture.md and board-sweep.md invoke batch scripts by a bare `scripts/...` path that won't resolve from a… | `skills/jared/references/context-capture.md:65,71,74,77 and skills/jared/references/board-sweep.md:33,102` |
| **F53** | P2 | 1c | Hand-written `degraded:` example strings don't match the canonical phrasing (two more surviving instances) | `skills/jared/references/new-board.md:56` |
| **F54** | P2 | 1c | new-board.md repeats the false "jared close polls for auto-move" claim (separate from the already-filed SKILL.md… | `skills/jared/references/new-board.md:64` |
| **F55** | P2 | 1c | pii-pre-flight.md lists two pre-flight consumers; the code has three (_cmd_close also calls it) | `skills/jared/references/pii-pre-flight.md:34` |
| **F56** | P2 | 1c | session-continuity.md's 'iff board has Session start checks configured' claim omits the required… | `skills/jared/references/session-continuity.md:175` |
| **F57** | P2 | 1c | structural-review.md's backend-gate degraded: strings omit the canonical 'on <backend>' clause | `skills/jared/references/structural-review.md:4-6` |
| **F58** | P2 | 1c | voice.md's boundary table misattributes 'drift-reconcile prompts' to /jared-groom | `skills/jared/references/voice.md:16` |
| **F59** | P2 | 1c | voice.md's voice-OFF batch-script list is incomplete relative to SKILL.md's own CLI-string policy | `skills/jared/references/voice.md:24` |

### P1 — must-fix before release

#### F3 · P1 · dim 1a — kf_number_index reseed silently collapses duplicate task numbers with no detection, contradicting its own documented recovery claim

- **Location:** `skills/jared/scripts/lib/kanbanflow_provider.py:233-243,391-393 (contradicted claim at skills/jared/scripts/lib/kf_number_index.py:12-14)`
- **Claim:** kf_number_index.py's own docstring asserts that when two tasks end up sharing the same jared #N, 'a reseed scan + manual renumber repairs it.' That is false as implemented: KanbanFlowProvider._reseed_index rebuilds the number->task_id map with a plain dict comprehension over every task on the board, keyed by number_value, with no collision check -- if two live tasks share a number, one _id is silently and permanently dropped from the index with no error, warning, or log, so nothing signals a collision for an operator to 'manually renumber.' The exposure is not limited to the two-concurrent-`jared file`-calls race the docstring calls out: `_ensure_seeded` (kanbanflow_provider.py:241-243) only triggers a reseed when the on-disk index `is_empty()`; a non-empty-but-stale index (e.g., a task numbered outside jared's knowledge appears on the live board after the local index was last built)…
- **Evidence:** kf_number_index.py:12-14: 'Two concurrent `jared file` calls may both pick max+1 and collide (last-writer-wins on the index); a reseed scan + manual renumber repairs it.' kanbanflow_provider.py:233-239 (`_reseed_index`): `mapping = {\n t.number_value: t.id\n for t in self._client.iter_all_tasks()\n if t.number_value is not None\n}\nself._index.replace(mapping)` -- last write for a repeated key wins, silently. kanbanflow_provider.py:241-243 (`_ensure_seeded`): `if self._index.is_empty():\n self._reseed_index()` -- skips reseeding whenever the index has any entries at all, regardless of staleness. kanbanflow_provider.py:391-393 (`_next_number`): `self._ensure_seeded()\n return self._index.max_number() + 1` -- trusts whatever max the (possibly stale) local index holds. tests/test_kanbanflow_provider.py:224-228 (`test_file_seeds_next_number_from_existing_tasks`) only exercises the…
- **Suggested fix:** Make _reseed_index detect and surface (raise, log, or return) any number_value that maps to more than one task id instead of silently picking a winner, and/or give KfNumberIndex a staleness signal (e.g. compare local max_number() against a cheap live check, or reseed unconditionally on a TTL) so _next_number doesn't hand out numbers purely from a non-empty-but-possibly-stale on-disk map.
- **Verification:** 2 refute-first verifiers · 0 refuted · 0 uncertain.

#### F4 · P1 · dim 1a — migrate.py's cross-ref rewriter lacks ties.py's false-positive guard, corrupting text on kanbanflow->github

- **Location:** `skills/jared/scripts/lib/migrate.py:101 (rewrite_cross_refs, :104-116); direction gate at skills/jared/scripts/jared:1890,1917; invoked at jared:1945,1947`
- **Claim:** On a kanbanflow->github migration (the direction where GitHub reassigns numbers, producing a real old->new map, unlike GH->KF's identity map), rewrite_cross_refs's regex `_ISSUE_REF_RE = re.compile(r"#(\d+)\b")` has no guard against '#N' immediately preceded by an alphanumeric character. The sibling regex for the identical concept in ties.py deliberately adds that guard: `_ISSUE_REF_RE = re.compile(r"(?<![A-Za-z0-9])#(\d+)\b")`. So during migration, text like a URL fragment (`docs/output.html#42`) or a compound ID (`ABC123#42`) gets misread as a genuine issue cross-reference and silently rewritten whenever the captured number happens to be an old issue number in the ledger's number map -- corrupting migrated body/comment text (e.g. breaking a hyperlink anchor) with zero warning.
- **Evidence:** Empirically verified: `re.compile(r'#(\d+)\b').findall('See docs/output.html#42 for details')` -> `['42']`; `re.compile(r'(?<![A-Za-z0-9])#(\d+)\b').findall(same)` -> `[]`. Same divergence for `'ticket ABC123#42 needs triage'`. migrate.py:107-109's own docstring claims 'the false-positive guard for #N tokens that are not migrated issue refs (Q2)' exists, but the only implemented guard is 'number absent from the map,' not the preceding-character check. Direction scoping confirmed via jared:1890 `preserve_number = direction == "github->kanbanflow"` and jared:1917 `number=item.number if preserve_number else None` -- GH->KF yields ledger.mark(old=N,new=N) (no-op rewrites), KF->GitHub yields a genuinely different new number (the corrupting case). tests/test_migrate_core.py's `test_rewrite_cross_refs_is_word_boundaried` only tests the trailing boundary ('#1' vs '#10'), never a…
- **Suggested fix:** Port ties.py's `(?<![A-Za-z0-9])` lookbehind into migrate.py's `_ISSUE_REF_RE` (or share one regex between the two modules).
- **Verification:** 2 refute-first verifiers · 0 refuted · 0 uncertain.

#### F5 · P1 · dim 1a — blocked-by edges to a closed/Done endpoint are silently dropped, not converted -- and undisclosed at all on kanbanflow->github

- **Location:** `skills/jared/scripts/lib/migrate.py:54-78 (compute_loss_axes), :119-128 (translate_edges); skills/jared/scripts/jared:2008-2017 (--include-closed inert), :2036-2039 (axis count); skills/jared/scripts/lib/board.py:1180-1183; skills/jared/scripts/lib/github_provider.py:429-447; skills/jared/scripts/lib/kanbanflow_provider.py:272-283 vs :288-295; skills/jared/scripts/lib/kanbanflow_client.py:467-472`
- **Claim:** jared migrate only ever migrates open/non-Done items (--include-closed is explicitly inert), but fetch_blocked_by_edges() on both backends returns edges regardless of the endpoint's open/closed state: GitHub's GraphQL query has no state filter on blockedBy nodes and the provider wrapper explicitly discards the state it does fetch; KanbanFlow's fetch_blocked_by_edges iterates iter_all_tasks() (every column, Done included) unlike its sibling list_open_items() which explicitly excludes Done. So on either direction an edge whose blocker (or dependent) is closed/Done is fetched into `edges`, but translate_edges silently drops any edge with an unmapped endpoint -- the edge just vanishes, it is not converted into anything. Disclosure is inconsistent and, in one direction, absent: on github->kanbanflow the native_dependencies axis fires and its count is set to the FULL len(edges), with wording…
- **Evidence:** board.py:1180-1183 GraphQL: `issues(...,states:OPEN){...nodes{number blockedBy(first:20){nodes{number state}}}}` -- dependent side is OPEN-filtered, blocker side (`blockedBy` nodes) is not. github_provider.py:429-447's fetch_blocked_by_edges maps these to Edge without checking state ('`state` is discarded' per its own docstring). kanbanflow_provider.py:272-283 list_open_items: `if t.column_id != done_id and t.number_value is not None` (excludes Done); :288-295 fetch_blocked_by_edges: `for task in self._client.iter_all_tasks(): ...` (no such filter); kanbanflow_client.py:467-472 iter_all_tasks iterates `board.columns` unrestricted. Read the authoritative lossiness record before asserting this gap: docs/superpowers/specs/archived/2026-06/2026-06-02-board-provider-abstraction-design.md 'Appendix A -- KanbanFlow mapping' / 'Migration fidelity' sections name only renumbering (KF->GH),…
- **Suggested fix:** Before rendering the report, compute how many fetched edges have an endpoint outside the migrated items set (a set-membership check against {i.number for i in items}) and surface that count as its own named loss line in both directions -- including kanbanflow->github, where nothing currently fires.
- **Verification:** 2 refute-first verifiers · 0 refuted · 0 uncertain.

#### F6 · P1 · dim 1a — stage.py computes "today" from local system clock, not UTC, mismatching GitHub's UTC timestamps

- **Location:** `skills/jared/scripts/stage.py:521 (source of the mismatch), consumed at lines 89-101 (milestone_proximity_days) and 104-118 (days_in_backlog)`
- **Claim:** `main()` sets `today = date.today()` (local calendar date) and threads it into `days_in_backlog`/`milestone_proximity_days`, which subtract it against dates parsed from GitHub's UTC `createdAt`/`due_on` timestamps. For any operator not in a UTC-or-ahead timezone, this produces a systematically wrong day count (up to a full day) for a large fraction of every 24-hour period.
- **Evidence:** Confirmed live on this exact machine: `python3 -c "from datetime import date, datetime, UTC; print(date.today(), datetime.now(UTC).date())"` printed `2026-09-11` vs `2026-09-12` (system timezone is America/Los_Angeles, currently PDT −07:00, verified via `timedatectl`). stage.py:118 `return (today - created_date).days` and stage.py:101 `return float((due_date - today).days)` both take `created_date`/`due_date` from `datetime.fromisoformat(<UTC ts>.replace("Z", "+00:00")).date()` (lines 115, 98) — a UTC calendar date — while `today` (line 521, `today = date.today()`) is the *local* calendar date. tests/test_stage.py never exercises this: every test passes `today=date.today()` explicitly alongside locally-constructed fixture dates, so the UTC-vs-local mismatch never surfaces in the unit suite; it only manifests when `main()`'s `today` is compared against real GitHub UTC timestamps.
- **Suggested fix:** Compute `today = datetime.now(UTC).date()` in stage.py's `main()` instead of `date.today()`, matching sweep.py's convention (`dt.datetime.now(dt.UTC)`) used throughout that script for the same class of comparison.
- **Verification:** 2 refute-first verifiers · 0 refuted · 0 uncertain.

#### F7 · P1 · dim 1b — jared wrap-state's gh pr view call skips the GH_TOKEN/GITHUB_TOKEN scrub, and a failure silently reads as "no PR"

- **Location:** `skills/jared/scripts/jared:1585-1606 (contrast with lib/board.py:987-999 _child_env, and the invariant claim at lib/board.py:1039-1042/1066-1067)`
- **Claim:** _cmd_wrap_state (the `jared wrap-state` subcommand) invokes `gh pr view` via a bare `subprocess.run([...])` with no `env=` kwarg, so it does NOT get the GH_TOKEN/GITHUB_TOKEN scrub that `_child_env()` provides everywhere else `gh` is shelled out to (board.py:847, 1091, 1120 all pass `env=_child_env()`; this is the only `gh` invocation in the tree that doesn't). board.py's own diagnostic text asserts as a blanket fact that 'jared scrubs GH_TOKEN/GITHUB_TOKEN before invoking gh' (see the comment at board.py:988-994: 'Scrubbing here forces project mutations (and every other gh call) onto the OAuth session jared expects to be authoritative' and the operator-facing line at board.py:1066-1067: 'jared scrubs GH_TOKEN/GITHUB_TOKEN before invoking gh — see #65'). That claim is false for this call path. The consequence is not a leak but a silent wrong outcome: if a GH_TOKEN/GITHUB_TOKEN in the…
- **Evidence:** skills/jared/scripts/jared:1585-1597: ``` pr_result = subprocess.run( [ "gh", "pr", "view", "--json", "number,mergeable,mergeStateStatus,state,statusCheckRollup,reviewDecision", branch, ], capture_output=True, text=True, check=False, ) ``` No `env=` argument — contrast with lib/board.py:1120-1129 (`run_gh_raw`): `result = subprocess.run(full_args, capture_output=True, text=True, check=False, env=_child_env(), input=input_text)`. The swallow: jared:1599-1606: ``` if pr_result.returncode != 0: pr_state = PrState( exists=False, pr_number=None, checks_status="none", mergeable=None, merged=False, ) ``` And the invariant text it violates, lib/board.py:1066-1067: `"Token source used: gh auth login OAuth session " "(jared scrubs GH_TOKEN/GITHUB_TOKEN before invoking gh — see #65)."` Confirmed via `grep -n '"gh"' skills/jared/scripts/jared skills/jared/scripts/lib/board.py` that jared:1587 is…
- **Suggested fix:** Route this call through `run_gh`/`run_gh_raw` (which already applies `_child_env()`), or at minimum pass `env=_child_env()` directly, so `jared wrap-state` honors the same GH_TOKEN/GITHUB_TOKEN scrub as every other gh call in the codebase.
- **Verification:** 2 refute-first verifiers · 0 refuted · 0 uncertain.

#### F8 · P1 · dim 1c — SKILL.md's 'jared close polls for auto-move' claim contradicts tested implementation (no poll)

- **Location:** `skills/jared/SKILL.md:116, :204, :206`
- **Claim:** SKILL.md states three times that `jared close` polls GitHub for the board's auto-move to Done and only falls back to an explicit Status=Done set if the poll doesn't observe the auto-move firing. In reality, `GitHubProjectsProvider.close()` never polls or checks prior state at all — it unconditionally calls `gh issue close` then unconditionally force-sets Status=Done every time, and this 'no poll' behavior is explicitly asserted by a passing unit test.
- **Evidence:** SKILL.md:206 — "the CLI closes the issue and polls for the board's auto-move to Done, falling back to an explicit `Status=Done` set if the auto-move hasn't fired." SKILL.md:204 — "The auto-move poll below is GitHub Projects v2-specific... `degraded: CLOSED_STATE unavailable — auto-move polling is GitHub Projects v2-specific; close sets Done directly on this backend`". SKILL.md:116 — "closing with verification (close + confirm auto-move)". Actual code, skills/jared/scripts/lib/github_provider.py:878-920, docstring: "Mirrors _cmd_close: optional comment → gh issue close → set_field (always, per #137 defense-in-depth; no poll)." and body comment at line 917-920: "# Defense-in-depth (#137): always set Status=Done explicitly; cheap no-op if the GitHub \"Item closed → Done\" workflow fired, genuine save if it didn't." `self.set_field(ref, "Status", "Done")` runs unconditionally, with no…
- **Suggested fix:** Rewrite SKILL.md:116, :204, :206 to describe the actual mechanism: close always force-sets Status=Done immediately after `gh issue close` (a cheap no-op if the board's built-in workflow already moved it, a real fix if it didn't) — there is no polling or wait loop on either backend. Drop 'poll'/'polling'/'confirm auto-move' language throughout.
- **Verification:** 2 refute-first verifiers · 0 refuted · 0 uncertain.

#### F9 · P1 · dim 1c — session-handoff-prompt config documents a /jared-wrap file-write feature that has no consumer and is contradicted elsewhere

- **Location:** `skills/jared/assets/project-board.md.template:125`
- **Claim:** The template documents `- session-handoff-prompt: ask` as a live, wired-up feature: "when `/jared-wrap` finishes, ask whether to draft a session handoff prompt for the next session... Used by `/jared-wrap`. The prompt is written to `tmp/next-session-prompt-<TIMESTAMP>.md` and is `.gitignore`d." This is stale/aspirational doctrine, not shipped behavior — the field is parsed but never read by anything that would act on it, and the actual `/jared-wrap` command explicitly denies doing this.
- **Evidence:** Quoted line (project-board.md.template:125): "- `session-handoff-prompt: ask` — when `/jared-wrap` finishes, ask whether to draft a session handoff prompt for the next session. Values: `ask` (default), `always`, `never`. Used by `/jared-wrap`. The prompt is written to `tmp/next-session-prompt-<TIMESTAMP>.md` and is `.gitignore`d — ephemeral by design." Cross-checked: `skills/jared/scripts/lib/board.py:65,178,202,241` parse `session-handoff-prompt` into `Board.session_handoff_prompt`, but `grep -rn session_handoff_prompt skills/jared/scripts/jared` returns nothing (the CLI never reads the field), and `grep -rn session-handoff-prompt commands/` returns nothing (no slash-command stub reads it as prose either). Decisively, `commands/jared-wrap.md:12` (the actual consumer surface) says the opposite: "The session record lives on the touched issues as Session-note comments — no `tmp/` file is…
- **Suggested fix:** Either remove the `session-handoff-prompt` bullet and its parsing/tests entirely (the #114 precedent: revert doc + parsing together), or actually wire `/jared-wrap` to read and act on it. Until one happens, the template is actively misleading a new project's Jared config.
- **Verification:** 2 refute-first verifiers · 0 refuted · 0 uncertain.

#### F10 · P1 · dim 1c — dependencies.md misdescribes dependency-graph.py's KanbanFlow fallback as reading label emulation; it actually falls back to `## Blocked by` body-text parsing

- **Location:** `skills/jared/references/dependencies.md:7`
- **Claim:** The doc states that on a NATIVE_DEPENDENCIES-absent backend, "`dependency-graph.py` native edges — reads label-emulation", implying it consumes the `blocked-by:<N>` label markers that `jared blocked-by` writes on KanbanFlow. The actual code does not read those labels at all; it falls back to parsing the `## Blocked by` issue-body prose section, a completely different (and manual) data source.
- **Evidence:** Quoted line (dependencies.md:7): "**`dependency-graph.py` native edges** — reads label-emulation: `degraded: NATIVE_DEPENDENCIES unavailable — dependency-graph.py reads label emulation; output is advisory only, not backed by native relation edges` (NATIVE_DEPENDENCIES absent)." Actual code (`skills/jared/scripts/dependency-graph.py:158-172`): the docstring says "Phase 6: when `board` is provided and NATIVE_DEPENDENCIES is absent, emits a degradation note to stderr and returns None so the caller falls through to body-text parsing", and the `degraded_or_none` call's `instead` string is literally "native edges unavailable — falling back to `## Blocked by` body-text parsing" (line 171). `grep -n "blocked-by:\|label.*emulat\|body-text pars\|## Blocked by" skills/jared/scripts/dependency-graph.py` shows zero label-parsing code anywhere in the file — only the body-text-parsing fallback (also…
- **Suggested fix:** Correct the bullet to say dependency-graph.py falls back to parsing the `## Blocked by` body-text section (not label emulation) when native edges are absent — and note that this means `blocked-by:<N>` label markers set by `jared blocked-by` on KanbanFlow are NOT currently consumed by dependency-graph.py, which is worth flagging as a real functional gap for KanbanFlow operators relying on the graph.
- **Verification:** 2 refute-first verifiers · 0 refuted · 0 uncertain.

#### F11 · P1 · dim 1c — Backend-gate boxes across reference docs quote fabricated `degraded: ...` strings that never appear in the code

- **Location:** `skills/jared/references/jared-cli.md:114,254`
- **Claim:** jared-cli.md tells the reader that `jared close` and `jared blocked-by`, on a KanbanFlow (CLOSED_STATE/NATIVE_DEPENDENCIES-absent) backend, will print a specific literal `degraded: ...` line. Neither literal string is ever printed by the code — the same fabricated-quote pattern (capability's ALL-CAPS enum name substituted for the real human-readable `feature` string, `on <backend>` omitted, and an invented `instead` clause) repeats near-identically across most other reference docs' "Backend gate" boxes.
- **Evidence:** jared-cli.md:114 quotes: "degraded: CLOSED_STATE unavailable — auto-move polling is GitHub-specific; jared close sets Done directly without polling on this backend". jared-cli.md:254 quotes: "degraded: NATIVE_DEPENDENCIES unavailable — on KanbanFlow, jared blocked-by uses label emulation; edges do not appear in a native panel". Neither string exists anywhere: `grep -rn "auto-move polling is GitHub-specific\|does not appear in a native panel\|CLOSED_STATE unavailable\|NATIVE_DEPENDENCIES unavailable" skills/jared/scripts/` returns zero hits. Reading the actual implementations (`skills/jared/scripts/jared:519-555` `_cmd_close`, and the blocked-by handler at `:835-856`) shows the real degradation note is only ever computed via `degraded_or_none(board, Capability.CLOSED_STATE, "native close state", "no native closed state on this backend")` / `degraded_or_none(board,…
- **Suggested fix:** Stop hand-authoring literal `degraded: ...` quotes in prose docs. Either point to the real per-call-site feature/instead strings actually in the source, or state the behavior without quoting fake exact text — since CLAUDE.md already documents these reference surfaces as intentionally NOT calling `degraded_or_none` (they branch on the `- backend:` bullet directly), the quoted strings should be dropped or clearly marked as illustrative rather than presented as literal CLI output.
- **Verification:** 2 refute-first verifiers · 0 refuted · 0 uncertain.

#### F12 · P1 · dim 1d — README Prerequisites never mentions Python or git, both hard runtime dependencies of every slash command

- **Location:** `README.md:44-62 (### Prerequisites section)`
- **Claim:** The Prerequisites section a stranger is told to satisfy before installing lists exactly three things — Claude Code, an authenticated `gh` CLI with `project` scope, and a GitHub Projects v2 board — and never mentions that a Python interpreter (3.11+, per the companion finding) must be on PATH, even though every single slash command shells out to `${CLAUDE_PLUGIN_ROOT}/skills/jared/scripts/jared` or `.../stage.py`, both `#!/usr/bin/env python3`-shebang Python scripts, nor that `git` must be installed, even though `/jared-start` (run for every session, not just multi-session ones) shells out to `git rev-parse --git-common-dir`. A stranger without a working `python3` on PATH, or without `git`, gets no warning from the README and will hit a bare shell error (`/usr/bin/env: python3: No such file or directory` or `git: command not found`) on their very first command, with nothing in the docs…
- **Evidence:** README.md:44-62 — Prerequisites bullets are exactly: "**Claude Code.**", "**`gh` CLI, authenticated.**", "**GitHub token with `project` scope.**", "**A GitHub Projects v2 board.**" — no Python/git bullet. skills/jared/scripts/jared:1 — `#!/usr/bin/env python3`. skills/jared/scripts/stage.py:1 — `#!/usr/bin/env python3`. commands/jared-start.md:42-43 — `GIT_COMMON_DIR=$(git rev-parse --git-common-dir 2>/dev/null)` / `REPO_ROOT=$(realpath "$(dirname "$GIT_COMMON_DIR")")`, run unconditionally in `/jared-start`'s "Resolve the repo's lock directory" step, before the `--session`/`--no-worktree` branching. A repo-wide grep of README.md and docs/getting-started.md for "python" (case-insensitive) returns only one incidental hit, README.md:334 ("Slash commands sit on top of a unified Python CLI" — descriptive prose, not a Prerequisites bullet, and not in the Prerequisites section at all).
- **Suggested fix:** Add two bullets to README's Prerequisites: "Python 3.11+ on PATH" (with a one-line note that the CLI is a Python script) and "git installed" (jared-start resolves the repo root via git).
- **Verification:** 2 refute-first verifiers · 0 refuted · 0 uncertain.

#### F13 · P1 · dim 1d — Stage-0's "proven Python floor ≤3.9" is a false positive — datetime.UTC (3.11+-only) breaks /jared-stage and jared summary on 3.9/3.10

- **Location:** `skills/jared/scripts/stage.py:20; skills/jared/scripts/lib/github_provider.py:645; skills/jared/scripts/lib/board.py (7 call sites incl. :1468,:1556,:1873,:1927-28,:2004,:2007); skills/jared/scripts/jared:1484; skills/jared/scripts/lib/kanbanflow_provider.py:322; skills/jared/scripts/sweep.py (6 call sites)`
- **Claim:** Task item asked to rule on whether the declared `requires-python = ">=3.11"` is deliberate or an unexamined-and-too-strict default. Having read the actual runtime code, the floor is in fact necessary, not too strict — but Stage-0's own clean-room verdict that the code is Python-3.9-compatible is false. `datetime.UTC` is a Python-3.11-only addition (confirmed against the CPython 3.11 changelog: "Add datetime.UTC, a convenience alias for datetime.timezone.utc. (gh-91973)" — absent on 3.9/3.10). `stage.py:20` does `from datetime import UTC, date, datetime` at module top level, so merely invoking `/jared-stage` (which runs `stage.py` directly, its own script with its own `if __name__ == "__main__"`) raises `ImportError: cannot import name 'UTC' from 'datetime'` on 3.9/3.10 before argparse even runs. Separately, `github_provider.py`'s `recently_closed()` does a local `from datetime import…
- **Evidence:** stage.py:20 — `from datetime import UTC, date, datetime` (top-level import, unconditional). github_provider.py:645 (inside `def recently_closed(self, *, days: int) -> list[ClosedItem]:`) — `from datetime import UTC, datetime, timedelta` then `cutoff = (datetime.now(UTC) - timedelta(days=days)).strftime("%Y-%m-%d")`. jared:940-1005 `_cmd_summary` calls `_detect_stuck_closed_recent(board, days=14)` (jared:972) which (absent the CLOSED_STATE degradation, i.e. on GitHub) calls `_fetch_recently_closed` → `board.provider.recently_closed(days=days)` (jared:1096,1135-1143). jared:128 wires `summary.set_defaults(func=_cmd_summary)`. stage0_ground_truth.json:20-21 states the proven floor as `<=3.9 (COLD-OK on 3.9-3.14, stdlib-only)` and stage0's own `help_probe` note (`"note": "All 19 --help succeed; parser loads with no MCP and no auth."`) shows the probe was `--help` only. CPython 3.11 What's…
- **Suggested fix:** (1) Correct the Stage-0/ledger record: the true floor is >=3.11, proven necessary by datetime.UTC on the /jared-stage and jared-summary hot paths, not <=3.9 — the existing pyproject.toml declaration is right and should be kept, not loosened. (2) Because that declaration is otherwise inert for a marketplace-installed user (shebang execution never consults pyproject.toml), add an explicit `sys.version_info < (3, 11)` guard near the top of skills/jared/scripts/jared's entry point (and…
- **Verification:** 2 refute-first verifiers · 0 refuted · 0 uncertain.

#### F14 · P1 · dim 1f — getting-started.md states the wrong WIP cap default (3 vs. the actual 4)

- **Location:** `docs/getting-started.md:107 (contradicted by README.md:37-38, commands/jared-start.md:89, skills/jared/SKILL.md:285, skills/jared/scripts/sweep.py:716, and CHANGELOG.md:123; root cause in skills/jared/scripts/bootstrap-project.py:732)`
- **Claim:** The flagship 'first 15 minutes' walkthrough tells a newcomer the In-Progress WIP cap is 3, but every other authoritative source (README, SKILL.md, the actual jared-start doctrine, and the sweep.py runtime default) says 4. This is a real, tracked regression: issue #245 raised the default from 3→4 and README/SKILL.md/jared-start.md/sweep.py were all updated, but bootstrap-project.py's --wip-limit default (still 3) and docs/getting-started.md (still '3') were never updated to match, so every freshly-bootstrapped project's generated docs/project-board.md also bakes in the stale '~3' figure.
- **Evidence:** docs/getting-started.md:107: "Check WIP (default cap: 3 In Progress at once)." vs. commands/jared-start.md:89: "Compare `M` ... against the project's configured cap (default 4, per #245)." vs. README.md:37-38: "**WIP cap on In Progress** (default 4, counted in workstreams...)" vs. skills/jared/scripts/sweep.py:716: `parser.add_argument("--wip-limit", type=int, default=4, help="In Progress cap")`. Root cause: skills/jared/scripts/bootstrap-project.py:732: `parser.add_argument("--wip-limit", type=int, default=3)` — never bumped when CHANGELOG.md:123 recorded "Default WIP caps raised to **8 Up Next / 4 In Progress** (was 5/3). (#245)".
- **Suggested fix:** Bump bootstrap-project.py's --wip-limit default to 4 (matching #245), fix the '~{wip_limit}' text baked into freshly-generated docs/project-board.md, and fix docs/getting-started.md:107 to say 4.
- **Verification:** 2 refute-first verifiers · 0 refuted · 0 uncertain.

#### F15 · P1 · dim 1f — getting-started.md tells a newcomer to type bare `jared` CLI commands into their shell, but nothing in the documented install flow ever puts `jared` on PATH or explains how to invoke it outside a Claude Code session

- **Location:** `docs/getting-started.md:30-32, 135, 161 (install flow: README.md:64-76)`
- **Claim:** The documented install path is exclusively `/plugin marketplace add` + `/plugin install jared` inside Claude Code chat, which places the plugin's files under Claude Code's own plugin cache (an 'implementation detail' per CLAUDE.md, explicitly not to be hardcoded). Nowhere does README.md or getting-started.md explain how a bare `jared` command becomes runnable in a real shell. Yet getting-started.md explicitly frames steps 4 and 5 as run 'in your shell' with a literal `jared comment ...` / `jared close ...` command and no `${CLAUDE_PLUGIN_ROOT}` prefix, no absolute path, and no 'ask Claude Code to run this for you' framing (unlike steps 1/2/3/6, which are each explicitly labeled 'In Claude Code:'). A stranger following the walkthrough literally would get a bare 'command not found: jared' at step 4, with nothing in either doc to search for.
- **Evidence:** docs/getting-started.md:30-32: "Everything is typed into Claude Code (slash commands) or your shell (the `jared` CLI). ... slash commands are the discoverable surface, the CLI is what slash commands call under the hood." Step 4 (line 135): "jared comment <your-issue-#> --body \"Started on the typo fix; localized to README line 47.\"" Step 5 (line 161): "jared close <your-issue-#>". Contrast with CLAUDE.md's own rule for invoking this exact script: "Use `${CLAUDE_PLUGIN_ROOT}/skills/jared/scripts/jared <subcommand>`. **Never hardcode `~/.claude/skills/...` paths** — the plugin cache location is an implementation detail of Claude Code's install system." — the only form the project's own internal docs (e.g. commands/jared-start.md:84, 'Run `${CLAUDE_PLUGIN_ROOT}/skills/jared/scripts/jared summary`') ever actually use is the CLAUDE_PLUGIN_ROOT-prefixed form, never a bare `jared`.
- **Suggested fix:** Either state explicitly in getting-started.md that steps 4/5 mean 'ask Claude Code to run this' (it has ${CLAUDE_PLUGIN_ROOT} in its session env), or add an install-time step that resolves/aliases jared onto PATH, and say so before the first bare `jared` invocation appears.
- **Verification:** 2 refute-first verifiers · 0 refuted · 0 uncertain.

#### F34 · P1 · dim 1a — append_decision uses substring containment, not equality, for its idempotency check — silently drops distinct decisions and reports success

- **Location:** `skills/jared/scripts/capture-context.py:150-171 (specifically lines 162-164)`
- **Claim:** append_decision's duplicate check is `if entry.strip() in current: return`, i.e. Python substring containment on the whole Decisions section text — not equality against a specific prior entry, despite the comment above it claiming to guard against duplicating 'the exact same entry'. Any new --decision text whose full dated block ('### <today>\n<text>') happens to be a literal substring of text already recorded for the same day is silently discarded: the function returns without modifying `sections['Decisions']`. If that was the only requested change, `new_body == body` at line 250, so main() prints '(no changes — already up to date)' and exits 0 — telling the operator their new decision was already recorded when it was never written at all. Concrete repro: an earlier run recorded '### 2026-09-11\nChose Postgres because of JSON support and existing tooling. Also enabling connection…
- **Evidence:** Line 162: `# Check idempotency — don't duplicate the exact same entry`; line 163: `if entry.strip() in current:`; line 164: `return`. Line 250-252: `if new_body == body: print("(no changes — already up to date)"); return 0`.
- **Suggested fix:** Compare against the set of already-recorded dated entries by equality (e.g. split the Decisions body on '### ' sub-headings and check the new entry against that exact list), not substring containment against the whole section text.
- **Verification:** 2 refute-first verifiers · 0 refuted · 0 uncertain.

#### F35 · P1 · dim 1c — board-sweep.md's Up Next overstock threshold (>3) doesn't match sweep.py's actual cap (8), which has no override flag

- **Location:** `skills/jared/references/board-sweep.md:45 vs skills/jared/scripts/sweep.py:343,848`
- **Claim:** board-sweep.md tells the groomer to treat more than 3 items in Up Next as 'overstocked'. But the sweep.py check this section describes (module docstring line 15: '3. Up Next queue — size and pullable-top check', which board-sweep.md's own §3 heading mirrors) implements the cap as `limit: int = 8` (sweep.py:343), and `main()` calls it with no override — `check_up_next_size(items)` at sweep.py:848 — so the emitted finding only fires above 8, not 3. sweep.py exposes no `--up-next-cap` flag at all (confirmed: `grep add_argument sweep.py` lists only `--owner/--project/--repo/--wip-limit/--staleness-days/--blocked-aging-days/--doc-sync-days/--min-budget/--force`), so there is no way to make the tool match the doc's stated threshold. A groomer following board-sweep.md's '>3 is overstocked' guidance will flag things sweep.py itself considers healthy, and will see sweep.py stay silent well past…
- **Evidence:** board-sweep.md:45: "- More than 3 items in Up Next? Overstocked. Propose moving lower items back to Backlog." | sweep.py:343: "def check_up_next_size(items: list[dict[str, Any]], limit: int = 8) -> list[str]:" | sweep.py:848: "for line in check_up_next_size(items) or [\"Healthy\"]:" (no limit argument passed) | commands/jared-groom.md:17 corroborates that sweep.py is the tool for '...Up Next size...' under board-sweep.md's checklist. This is a distinct code path from stage.py's separate `--up-next-cap` (default 8 there too, per stage.py:371/500) used by `/jared-stage` — not the already-filed jared-stage.md WIP-cap finding.
- **Suggested fix:** Either change the doc's threshold to match sweep.py's actual constant (8), or change sweep.py's default to 3 and thread it through a --up-next-cap flag — whichever direction is intended, then keep them in sync.
- **Verification:** 2 refute-first verifiers · 0 refuted · 0 uncertain.

#### F36 · P1 · dim 1c — human-readable-board.md's Labels section documents a `blocked` label as valid, contradicting the project's own retired-label doctrine

- **Location:** `skills/jared/references/human-readable-board.md:112 vs CLAUDE.md and skills/jared/references/board-sweep.md:55`
- **Claim:** human-readable-board.md's Labels section lists `blocked` as a legitimate state label ('must pair with `## Blocked by` section in body'). This directly contradicts the project's own explicit, CLI-enforced invariant: the repo's CLAUDE.md states 'Status columns: Backlog / Up Next / In Progress / Blocked / Done. **Blocked is a Status column, never a label. Do not introduce a "blocked" label anywhere.**' and board-sweep.md itself (line 55, one of my four assigned docs) says: '(The old `blocked` label is retired. A `blockedBy` edge alone does not put an issue in the Blocked column into being either.)' A session reading only human-readable-board.md's Labels section (a plausible standalone reference lookup) would apply a label the rest of the doctrine says must never exist.
- **Evidence:** human-readable-board.md:111-112: "- `bug` / `enhancement` / `refactor` / `documentation` / `test` — type\n- `blocked` — state (must pair with `## Blocked by` section in body)" | CLAUDE.md (project root): "**Blocked is a Status column, never a label. Do not introduce a \"blocked\" label anywhere.**" | board-sweep.md:55: "(The old `blocked` label is retired. A `blockedBy` edge alone does not put an issue in the Blocked column. Items move to Blocked only when actively stuck after being pulled to In Progress.)"
- **Suggested fix:** Delete the `blocked` bullet from human-readable-board.md's Labels list (or replace it with an explicit note that Blocked is a Status column only, mirroring board-sweep.md:55).
- **Verification:** 2 refute-first verifiers · 0 refuted · 0 uncertain.

#### F37 · P1 · dim 1c — human-readable-board.md's issue-body skeleton shows a `## Depends on` / `## Blocks` shape the real template doesn't have, and calls `## Blocks` usable when dependencies.md says it's retired

- **Location:** `skills/jared/references/human-readable-board.md:39-69,81 vs skills/jared/assets/issue-body.md.template and skills/jared/references/dependencies.md`
- **Claim:** human-readable-board.md presents a literal code-block 'skeleton' of `assets/issue-body.md.template` ending in `## Depends on\n(none)` and `## Blocks\n(none)` sections, and separately describes both as still-live sections ('`## Depends on` / `## Blocks` — `#N` references. Used when native GitHub issue dependencies aren't available, or for cross-repo.'). The real template file has neither heading — it instead carries an HTML comment explaining that dependencies are tracked natively via `addBlockedBy`/`blockedBy` and a conditional instruction to add a `## Blocked by` section only when the issue is in the Blocked column. Worse, this doc's own sibling reference `references/dependencies.md` states outright that `## Blocks` is dead: "The `## Blocks` body section is retired. To express the inverse direction, add a `blockedBy` edge on the dependent issue instead." So human-readable-board.md…
- **Evidence:** human-readable-board.md:61-65 (skeleton): "## Depends on\n(none)\n\n## Blocks\n(none)" | human-readable-board.md:81: "`## Depends on` / `## Blocks` — `#N` references. Used when native GitHub issue dependencies aren't available, or for cross-repo." | Actual skills/jared/assets/issue-body.md.template (full file read) contains no `## Depends on` or `## Blocks` heading anywhere — only `<!-- Dependencies are tracked natively via GitHub's blockedBy API... Add a "## Blocked by" section ONLY when this issue is in the Blocked Status column. -->` | skills/jared/references/dependencies.md: "The `## Blocks` body section is retired. To express the inverse direction, add a `blockedBy` edge on the dependent issue instead." (`## Depends on` is separately described there as legitimate but non-authoritative cross-repo prose only, not a template placeholder defaulting to '(none)').
- **Suggested fix:** Update the skeleton code block in human-readable-board.md to match the real issue-body.md.template (native-dependency comment + conditional `## Blocked by`), and correct the 'What goes in each section' bullet to say `## Blocks` is retired (point solely at native blockedBy edges) while keeping `## Depends on` framed as prose-only cross-repo fallback per dependencies.md.
- **Verification:** 2 refute-first verifiers · 0 refuted · 0 uncertain.

#### F38 · P1 · dim 1c — migration.md documents a `--diff` flag on bootstrap-project.py that does not exist

- **Location:** `skills/jared/references/migration.md:11`
- **Claim:** "If `docs/project-board.md` ... doesn't exist, run `scripts/bootstrap-project.py` to generate it. If it exists but is stale, run with `--diff` to surface drift and propose updates."
- **Evidence:** skills/jared/scripts/bootstrap-project.py:711-756 (`build_parser`) defines only `--url, --backend, --repo, --output, --wip-limit, --no-create, --force, --non-interactive, --yes, --work-streams` — no `--diff` argument anywhere. `grep -rn -- '--diff' skills/ commands/ docs/*.md README.md` matches only this one line in the whole repo. The real staleness-surfacing behavior (bootstrap-project.py:1060-1111) is automatic on a plain re-run (no flag): it diffs the existing doc against the new content (or patches just the missing header bullets) and writes a `<output>.new` file with a printed unified diff for the operator to `mv` into place.
- **Suggested fix:** Drop `--diff`; describe the real, flag-free mechanism: re-running `bootstrap-project.py` against an existing `docs/project-board.md` auto-detects staleness/legacy shape and writes `<output>.new` plus a printed diff, applied via `mv` or `--force`.
- **Verification:** 2 refute-first verifiers · 0 refuted · 0 uncertain.

#### F39 · P1 · dim 1c — Reference docs give bare `scripts/*.py` invocations that fail from a real project checkout

- **Location:** `skills/jared/references/migration.md:11,55,92; skills/jared/references/new-board.md:21,135`
- **Claim:** migration.md and new-board.md tell the reader to run `scripts/bootstrap-project.py` and `scripts/sweep.py` as bare relative paths (e.g. new-board.md:21 `scripts/bootstrap-project.py \`, migration.md:92 "run a full `scripts/sweep.py`").
- **Evidence:** Jared is installed as a Claude Code plugin and invoked with CWD set to the *target project's* repo root (per CLAUDE.md: 'the consumer is Claude Code itself... not a Python application'; 'Use `${CLAUDE_PLUGIN_ROOT}/skills/jared/scripts/jared <subcommand>`. Never hardcode `~/.claude/skills/...` paths'). The scripts live at `skills/jared/scripts/bootstrap-project.py` / `skills/jared/scripts/sweep.py`, so a bare `scripts/bootstrap-project.py` only resolves if CWD happens to be inside `skills/jared/`, which it never is during real use. Every command stub that invokes the same scripts gets this right: commands/jared-init.md:32,38,86 and commands/jared-groom.md:17 use the full `${CLAUDE_PLUGIN_ROOT}/skills/jared/scripts/bootstrap-project.py` / `sweep.py` path, and SKILL.md:315 does likewise. migration.md and new-board.md are inconsistent with the convention established everywhere else in the…
- **Suggested fix:** Prefix every script invocation in these two docs with `${CLAUDE_PLUGIN_ROOT}/skills/jared/scripts/`, matching commands/jared-init.md, commands/jared-groom.md, and SKILL.md.
- **Verification:** 2 refute-first verifiers · 0 refuted · 0 uncertain.

#### F40 · P1 · dim 1c — new-board.md's blank-project Status list omits Blocked

- **Location:** `skills/jared/references/new-board.md:35`
- **Claim:** "For a genuinely blank project (just created), the script proposes creating: - **Status** field: Backlog / Up Next / In Progress / Done" — omits Blocked.
- **Evidence:** skills/jared/scripts/bootstrap-project.py:49 hardcodes `"Status": ["Backlog", "Up Next", "In Progress", "Blocked", "Done"]` and line 770 defines `_CANONICAL_STATUSES = ("Backlog", "Up Next", "In Progress", "Blocked", "Done")` — the script always proposes/creates 5 Status options, not 4. CLAUDE.md itself states 'Blocked is a Status column, never a label' as a hard invariant, which this doc's own enumeration of what gets created contradicts.
- **Suggested fix:** Change the bullet to "Status field: Backlog / Up Next / In Progress / Blocked / Done" to match bootstrap-project.py's actual default option set.
- **Verification:** 2 refute-first verifiers · 0 refuted · 0 uncertain.

#### F41 · P1 · dim 1c — parallel-sessions.md's per-worktree venv setup omits the dev extras needed to run tests

- **Location:** `skills/jared/references/parallel-sessions.md:102-103`
- **Claim:** "Each worktree needs its own `.venv` — `uv sync` or `uv pip install -e .` inside the new worktree before running tests."
- **Evidence:** pyproject.toml has no `[dependency-groups]` table, and pytest/ruff/mypy live only under `[project.optional-dependencies].dev`. Empirically verified: running `uv venv .venv && uv pip install -e /home/brockamer/Code/jared` (the doc's exact second alternative, no `[dev]` extra) leaves `pytest` uninstalled — `pip show pytest` reports "Package(s) not found: pytest" and `which pytest` returns nothing. `uv sync` alone would likewise skip the optional-dependencies `dev` group (uv only installs optional-dependencies with `--extra`/`--all-extras`). CLAUDE.md's own Developer Setup section requires `uv pip install -e ".[dev]" # installs pytest, ruff, mypy` for exactly this reason.
- **Suggested fix:** Change the guidance to `uv pip install -e ".[dev]"` (or `uv sync --extra dev`) so a fresh worktree's venv actually has pytest/ruff/mypy before the operator tries to run tests.
- **Verification:** 2 refute-first verifiers · 0 refuted · 0 uncertain.

#### F42 · P1 · dim 1c — session-continuity.md's orientation example fabricates sections next-session-prompt never emits, and contradicts the doc's own later description

- **Location:** `skills/jared/references/session-continuity.md:129-150 (vs. skills/jared/scripts/jared:1182-1288, and the doc's own line 175)`
- **Claim:** The doc presents a concrete 'orientation output' example that it says is 'the handoff' produced when '/jared-start invokes jared next-session-prompt' — but that example shows sections (Blocked, Aging) and formatting (a dated 'Last session (2026-04-18): "..."' line, an 'In Progress (2/3):' / 'Up Next (top 3):' header style) that _cmd_next_session_prompt never generates, while omitting 'Recently closed', which it always does generate. The doc's own later, more careful description of the same command (line 175) lists only 'In flight ... Top of Up Next, Recently closed ... and a Quick health check block' — with no Blocked or Aging section at all, directly contradicting its own worked example two paragraphs above.
- **Evidence:** session-continuity.md:132-148 shows: ``` Where we are (board state, 2026-04-19): In Progress (2/3): #14 [High/Infrastructure] Add excluded_employers config + prefilter Last session (2026-04-18): "2/3 tests passing, unpushed. Next: decide YAML ordering." ... Blocked: none. Aging: #18 (High Backlog) filed 2026-03-25, no activity — worth revisiting. ``` The actual `_cmd_next_session_prompt` (skills/jared/scripts/jared:1182-1288) only ever prints the literal headers `## In flight`, `## Top of Up Next`, optional `## Session-N staged`, `## Recently closed (last 7 days)`, optional `## Quick health check on session start`, and `## To start` — there is no Blocked section and no Aging section anywhere in the function. Its per-item renderer `_render_in_flight` (jared:1168-1179) prints only `- #{number} [{prio}] {title}` then, if present, ` Last session: "{note_one_liner}"` with no date — and…
- **Suggested fix:** Replace the worked example at session-continuity.md:129-150 with output that matches the actual `## In flight` / `## Top of Up Next` / `## Recently closed (last 7 days)` shape and the undated `Last session: "<next-action text only>"` line, and drop the fabricated Blocked/Aging sections (or explicitly note they come from a different command/step, if that's the real intent).
- **Verification:** 2 refute-first verifiers · 0 refuted · 0 uncertain.

### P2 — polish

#### F16 · P2 · dim 1a — wrap-state conflates any `gh pr view` failure (auth/network/rate-limit) with "no PR exists"

- **Location:** `skills/jared/scripts/jared:1599-1606 (_cmd_wrap_state)`
- **Claim:** `_cmd_wrap_state` treats every nonzero exit from `gh pr view` identically as `PrState(exists=False, ...)`, with no inspection of stderr and no distinction between "no PR for this branch" and a transient/auth/network failure.
- **Evidence:** Read directly: `if pr_result.returncode != 0: pr_state = PrState(exists=False, pr_number=None, checks_status="none", mergeable=None, merged=False,)` — `pr_result.stderr` is captured (`capture_output=True`) but never read or printed anywhere in this branch. `decide_next_step` (lib/wrap_state.py:89) returns `"create_pr"` whenever `not pr_state.exists`, and commands/jared-wrap.md:116-119 runs `gh pr create --title "$TITLE" --body "$BODY"` unconditionally on that step with no existing-PR re-check.
- **Suggested fix:** Only map to exists=False when stderr indicates "no pull requests found" (gh's actual no-PR message); otherwise print the captured stderr and return a non-zero/error state instead of silently downgrading to exists=False.
- **Verification:** 2 refute-first verifiers · 0 refuted · 0 uncertain.

#### F17 · P2 · dim 1a — PR check-status classification is fail-open: any unrecognized check state reports as "passed"

- **Location:** `skills/jared/scripts/jared:1619-1628 (_cmd_wrap_state)`
- **Claim:** checks_status is computed by testing set membership against two fixed 3-string lists (a "failed" set and a "pending" set); anything else present in a non-empty statusCheckRollup falls through to `checks_status = "passed"` rather than being treated conservatively.
- **Evidence:** Read directly: `statuses = {c.get("conclusion") or c.get("status") for c in rollup}`; `if "FAILURE" in statuses or "CANCELLED" in statuses or "TIMED_OUT" in statuses: checks_status = "failed"`; `elif "IN_PROGRESS" in statuses or "PENDING" in statuses or "QUEUED" in statuses: checks_status = "pending"`; `elif rollup: checks_status = "passed"`. There is no allowlist of known-good conclusions (e.g. SUCCESS) — the design defaults an unlisted value to "passed" (fail-open) rather than to "pending"/"failed" (fail-closed).
- **Suggested fix:** Invert the logic to fail-closed: classify as "passed" only when every status/conclusion is in an explicit success allowlist, and treat anything else as "pending" (or a new "unknown" status routed to surface_failure) rather than defaulting to passed.
- **Verification:** 2 refute-first verifiers · 1 refuted · 0 uncertain.
- **Weak survivor:** 1 of 2 verifiers refuted this. It survives under the plan's "keep iff fewer than a majority refute" rule, but it is among the least certain entries — re-check before acting.

#### F18 · P2 *(verifiers corrected down from P1)* · dim 1a — Dry-run KanbanFlow call estimate hardcodes comment_count=0, undermining the one number meant to catch a rate-limit failure before --apply

- **Location:** `skills/jared/scripts/jared:2040-2049 (call site); skills/jared/scripts/lib/migrate.py:131-142 (estimate_kf_calls); apply-side comment porting at jared:1941-1952; hard-failure path at skills/jared/scripts/lib/kanbanflow_client.py:406-414`
- **Claim:** estimate_kf_calls is documented as an 'Upper-bound KanbanFlow write calls for a GH->KF apply run ... so the operator knows whether the run fits KF's 1,000 req/hr window' and its formula explicitly adds '1 POST per comment.' Its only call site always passes `comment_count=0` and `extra_fields_per_item=1` as fixed literals instead of deriving them from the real item set, even though _apply_migration's second pass unconditionally does `for note in src.list_comments(item.number): ... tgt.comment(new, body)` for every comment on every item. On any board with comments (the normal case) the printed dry-run number omits those calls entirely, defeating its stated purpose. If the underactual estimate leads to hitting KF's limit mid-run, the failure is not silent: kanbanflow_client.py raises `KanbanFlowRateLimitError('request retries exhausted')` after retries are exhausted, a type not in…
- **Evidence:** jared:2040-2049: `estimate_kf_calls(item_count=len(items), extra_fields_per_item=1, edge_count=len(edges), comment_count=0)`. grep of tests/test_cmd_migrate.py for 'comment_count'/'kf_call'/'estimate' returns nothing -- no test exercises or pins this call site's inputs; tests/test_migrate_core.py's test only checks the pure function in isolation with explicit non-zero inputs supplied by the test itself.
- **Suggested fix:** Either compute real per-item comment counts before printing the estimate (accepting one extra list_comments call per item at dry-run time -- plausibly why it was hardcoded to 0), or, if that cost is unacceptable, change the report line to state explicitly that the number excludes comments/extra fields rather than presenting it as an upper bound.
- **Verification:** 2 refute-first verifiers · 0 refuted · 0 uncertain.

#### F19 · P2 · dim 1a+1c — jared CLI's top-level --help usage line omits two real subcommands (audit, propose-partition) that command stubs invoke

- **Location:** `skills/jared/scripts/jared:69-87 (ALL_SUBCOMMANDS), :109-113 (metavar); registered at :310 (`audit`) and :427 (`propose-partition`)`
- **Claim:** The hardcoded `ALL_SUBCOMMANDS` list used to build the top usage line's `{...}` metavar has 17 entries and is missing `audit` and `propose-partition` — exactly the two subcommands that `/jared-audit` (step 1: `jared audit fetch ...`) and `/jared-stage` (Session-N partitioning step 1: `jared propose-partition --sessions N`) depend on. The subcommands work fine (they're registered via real `sub.add_parser(...)` calls and appear correctly in the detailed 'positional arguments:' listing below), so this is help-text-only, not a functional break — but a stranger skimming `jared --help`'s usage line would not know these two exist.
- **Evidence:** `jared --help` output: `usage: jared [-h] [--board BOARD]\n {get-item,summary,set,move,close,comment,file,add-to-board,blocked-by,next-session-prompt,ties,session-resolve,session-lock-write,session-lock-clear,worktree-add,wrap-state,migrate}\n ...` — 17 names, no `audit`, no `propose-partition`. Source: `skills/jared/scripts/jared:69-87` defines `ALL_SUBCOMMANDS = ["get-item", "summary", "set", "move", "close", "comment", "file", "add-to-board", "blocked-by", "next-session-prompt", "ties", "session-resolve", "session-lock-write", "session-lock-clear", "worktree-add", "wrap-state", "migrate"]` (17 entries) and line 112: `metavar="{" + ",".join(ALL_SUBCOMMANDS) + "}"`. But `audit_p = sub.add_parser("audit", ...)` (line ~310) and `propose-partition` (referenced via `jared propose-partition --sessions K` help output) are real, working subcommands verified independently via `jared audit…
- **Suggested fix:** Add "audit" and "propose-partition" to the ALL_SUBCOMMANDS list (or derive the metavar from `sub.choices` after all add_parser calls instead of hand-maintaining a separate list, so this can't drift again).
- **Verification:** 2 refute-first verifiers · 0 refuted · 0 uncertain.
- **Note:** Found independently by the 1a and 1c finders — cross-dimension confirmation.

#### F20 · P2 · dim 1a — Git-remote repo fallback uses wrong directory for root-level PROJECT_BOARD.md

- **Location:** `skills/jared/scripts/lib/board.py:133 (Board.from_path)`
- **Claim:** Board.from_path() always derives the git-remote fallback directory as path.parent.parent, which is correct only for a 1-level-deep config path (docs/project-board.md, .github/project-board.md). For the root-level alternate location PROJECT_BOARD.md (0 levels deep), this lands ONE DIRECTORY ABOVE the actual repo root, where git's upward .git discovery can never find the real repository (it only walks up, never down into sibling subdirectories) — so the documented git-remote fallback silently returns None and the doc fails with a misattributed 'missing required field(s): Repo' error even when `git remote get-url origin` in the actual repo has the answer. (docs/maintainers/project-board.md, 2 levels deep, lands one level too shallow at docs/ instead of repo root, but is compensated because git's upward search still finds the real .git from there — so only the root-level PROJECT_BOARD.md…
- **Evidence:** Source: `repo_fallback = _infer_repo_from_git(path.parent.parent)` at board.py:133. Reproduced end-to-end: created a git repo at <tmp>/parent/repo with `git remote add origin https://github.com/someowner/somerepo.git` and a PROJECT_BOARD.md (no `- Repo:` bullet) inside it. `Board.from_path(<tmp>/parent/repo/PROJECT_BOARD.md)` raised `BoardConfigError: .../PROJECT_BOARD.md missing required field(s): Repo. Run /jared-init to bootstrap or patch the file.` Direct comparison: `_infer_repo_from_git(path.parent.parent)` (what the code calls) returned None; `_infer_repo_from_git(path.parent)` (the correct directory) returned 'someowner/somerepo'. Confirmed this is manual-placement-only, not hit via /jared-init: bootstrap-project.py's default --output is docs/project-board.md and it always fills in the `- Repo:` bullet, so the canonical bootstrap flow never exercises this fallback path for…
- **Suggested fix:** Compute the fallback directory per-candidate based on actual nesting depth (e.g. walk up from `path` until a `.git` is found, or track each DEFAULT_CONFIG_PATHS entry's depth), rather than hardcoding `path.parent.parent` for all four alternate locations.
- **Verification:** 2 refute-first verifiers · 0 refuted · 0 uncertain.

#### F21 · P2 · dim 1a — Repo: bullet accepted without owner/name validation, crashes downstream with a raw ValueError instead of BoardConfigError

- **Location:** `skills/jared/scripts/lib/board.py:174 (Board._parse) and every `self.repo.split("/", 1)` call site (e.g. board.py:633 open_items, :719 fetch_open_issues_for_ties, :1179 fetch_blocked_by_edges, :1382 fetch_project_items_batch, :1431 fetch_recent_comments_batch, :2025 fetch_audit_window)`
- **Claim:** `repo = find_optional(r"Repo:\s*(\S+)") or repo_fallback` only checks that the `- Repo:` bullet's value is a single non-whitespace token — it never validates the documented 'owner/repo' shape the rest of the module assumes (unlike `_infer_repo_from_git`'s regex, which does enforce owner/name). A malformed doc with e.g. `- Repo: jared` (missing the owner/ prefix) parses successfully into a valid-looking Board, then crashes much later with a bare, uncaught `ValueError: not enough values to unpack (expected 2, got 1)` at the first `owner, repo_name = self.repo.split("/", 1)` call — instead of the module's own `BoardConfigError` contract that every other malformed-doc case raises with a clear, actionable message.
- **Evidence:** Reproduced directly: parsed a doc with `- Repo: jared` (no slash) via `Board._parse(...)`; `b.repo` came back as `'jared'` with no error. Calling `b.open_items()` then raised `ValueError : not enough values to unpack (expected 2, got 1)` — confirmed via a direct interpreter run against the checked-out lib.board module. `grep -n 'repo.split("/"'` shows six call sites in board.py (lines 197, 633, 719, 1179, 1382, 1431, 2025) all making the same unvalidated assumption.
- **Suggested fix:** Validate the `Repo:` bullet's shape (contains exactly one '/', non-empty owner and name) in `_parse` alongside the existing missing_gh checks, and raise `BoardConfigError` with a clear message when it doesn't match — mirroring the validation `_infer_repo_from_git`'s regex already performs on the git-derived fallback.
- **Verification:** 2 refute-first verifiers · 0 refuted · 0 uncertain.

#### F22 · P2 *(verifiers corrected down from P1)* · dim 1a — fieldValues(first:20) / projectItems(first:10) have no truncation guard, unlike the outer issues connection — can silently null out Status/Priority or lose the item entirely

- **Location:** `skills/jared/scripts/lib/github_provider.py:191-239 (query definitions), :354-359 (the guard that exists), :367-369, :400-402, :490-495; skills/jared/scripts/lib/board.py:1338-1358 (_flatten_project_item_for_project)`
- **Claim:** The file explicitly guards against silent truncation on the OUTER `issues(first: 100)` connection (raises GhInvocationError if hasNextPage), but the nested `projectItems(first: 10)` and `fieldValues(first: 20)` connections used by the exact same queries (_OPEN_ITEMS_QUERY and _ISSUE_PROJECT_ITEM_QUERY) have no equivalent guard at all. Two confirmed consequences: (1) `projectItems(first: 10)` — if an issue's target-project membership isn't among the first 10 projectItems GitHub returns, `_flatten_project_item_for_project` (board.py:1338-1358) returns None because it never sees a node whose `project.number` matches; `list_open_items()` then silently `continue`s past the issue (github_provider.py:367-369, dropping it from the board view entirely), `get_item()` returns None — indistinguishable from 'never added to the board' (github_provider.py:400-402) — and `_find_item_id()` raises…
- **Evidence:** github_provider.py:198-215: `projectItems(first: 10) { nodes { id project { number } fieldValues(first: 20) { nodes { ... on ProjectV2ItemFieldSingleSelectValue { name field { ... on ProjectV2SingleSelectField { name } } } } } } }` — no pageInfo/hasNextPage requested on either connection. Compare to :191-192,354-359 where the outer `issues(states: OPEN, first: 100, ...)` DOES request `pageInfo { hasNextPage }` and the code raises GhInvocationError on truncation. board.py:1340-1344: `for node in project_items_node.get("nodes", []) or []: ... if (node.get("project") or {}).get("number") != project_number: continue` — returns None (line 1358) if no node in the fetched window matches, with no signal that the window may have been too small. github_provider.py:367-369: `flat = _flatten_project_item_for_project(issue.get("projectItems"), self.project_number); if flat is None: continue`.…
- **Suggested fix:** Add the same hasNextPage guard already used for the outer issues connection to projectItems and fieldValues (request pageInfo{hasNextPage} on both nested connections and raise/warn when true), or bump the page sizes and add a defensive check. At minimum, add a test fixture where fieldValues.nodes contains non-single-select-type entries ahead of Status/Priority to confirm the current window is actually sufficient in practice.
- **Verification:** 2 refute-first verifiers · 0 refuted · 0 uncertain.

#### F23 · P2 · dim 1a — Transient-read retry (#342) covers migrate's reads but not get_item()/_find_item_id(), which query the identical ProjectV2 field-value shape

- **Location:** `skills/jared/scripts/lib/github_provider.py:382-405 (get_item), :474-495 (_find_item_id), vs. :91-104 (module docstring describing the flakiness) and :350-351/:425/:437/:457-458 (the four retried reads)`
- **Claim:** This is a resilience asymmetry, not a demonstrated defect — I want to be explicit that I have not confirmed the flakiness actually reaches this code path; I'm flagging the gap in coverage, not a proven failure. #342 (git history: 'retry transient GitHub source reads on the migrate path') deliberately wrapped only list_open_items, get_body, fetch_blocked_by_edges, and list_comments in _retry_transient_read, because those are migrate's GitHub source reads. get_item() and _find_item_id() run the same _ISSUE_PROJECT_ITEM_QUERY shape (projectItems→fieldValues, the same ProjectV2 field read the module docstring names as the source of the intermittent 401) but are not wrapped in the retry, and _find_item_id() in particular is the read every write path funnels through (_add_existing, set_field → move/close). The scope decision was explicit and intentional per the PR title, so this may be a…
- **Evidence:** github_provider.py:91-104 module docstring: 'GitHub returns an intermittent `read:project` scope enforcement on ProjectV2 fields, which 401s on one call and succeeds seconds later' — scoped to migrate's four named reads. github_provider.py:474-495 `_find_item_id` calls `run_graphql(_ISSUE_PROJECT_ITEM_QUERY, ...)` directly with no _retry_transient_read wrapper. github_provider.py:382-405 `get_item` likewise calls `run_graphql` directly. git log confirms scope: commit b3eabcf/a23e90c 'feat(jared): retry transient GitHub source reads on the migrate path (#342)'.
- **Suggested fix:** If the read:project flakiness is known to occur outside migrate (e.g., seen in jared set/move/close in the wild), extend _retry_transient_read to get_item() and _find_item_id() for consistency. If it's confirmed migrate-only, no action needed — but consider a one-line comment on get_item()/_find_item_id() noting the deliberate exclusion so a future reader doesn't assume it was missed.
- **Verification:** 2 refute-first verifiers · 0 refuted · 0 uncertain.

#### F24 · P2 *(verifiers corrected down from P1)* · dim 1a — KanbanFlow comment author falls back to a raw internal user-id, violating the documented "" contract

- **Location:** `skills/jared/scripts/lib/kanbanflow_provider.py:352-357 and :359-374 (contract at skills/jared/scripts/lib/board_provider.py:86-87)`
- **Claim:** The BoardProvider interface's Comment dataclass explicitly contracts that `author` is the display name "or '' if unresolved" (board_provider.py:86-87). KanbanFlowProvider._user_name violates that contract: when a comment's authorUserId isn't found in the board's current /users list (e.g. a departed/removed board member), it returns the raw KanbanFlow internal user id instead of "", leaking a provider-internal identifier across the BoardProvider boundary that Comment.author is supposed to keep opaque-or-empty. The sibling GitHubProjectsProvider honors the contract correctly (github_provider.py:463: `author=str((c.get("author") or {}).get("login") or "")`), confirming this is a KanbanFlow-side implementation bug, not an intentional interface difference.
- **Evidence:** board_provider.py:86-87 (Comment docstring): '`author` is the GitHub login or the KanbanFlow user's display name ("" if\n unresolved).' kanbanflow_provider.py:352-357: `def _user_name(self, user_id: str) -> str:\n if not hasattr(self, "_user_name_by_id"):\n self._user_name_by_id: dict[str, str] = {\n u.id: u.name for u in self._client.list_users()\n }\n return self._user_name_by_id.get(user_id, user_id)` -- the `.get(user_id, user_id)` fallback returns the raw id, not "". kanbanflow_provider.py:359-374 (`list_comments` docstring): 'Falls back to the raw id if the user is unknown.' -- an accurate description of the code, but one that directly contradicts the dataclass contract it's supposed to satisfy. Contrast: github_provider.py:463 `author=str((c.get("author") or {}).get("login") or "")` correctly falls back to "".
- **Suggested fix:** In _user_name, return "" (or fall through to "" in list_comments) when user_id is not present in _user_name_by_id, instead of defaulting to user_id itself, so the fallback matches the documented Comment.author contract and never exposes a raw KanbanFlow _id to callers.
- **Verification:** 2 refute-first verifiers · 0 refuted · 0 uncertain.

#### F25 · P2 · dim 1c — F2 adjudication: phase-tracking issues using bold **Deliverables** instead of ## Acceptance criteria — convention-doc clarification needed, not a code defect

- **Location:** `commands/jared-file.md:32-38 (canonical body template) vs skills/jared/scripts/stage.py:184-197 (is_epic exemption) and docs/project-board.md:124 (epic label semantics)`
- **Claim:** Ruling: this is a convention-doc clarification, not a real code defect. The project has a documented, code-backed exemption mechanism for issues that legitimately lack acceptance criteria — the `epic` label (`docs/project-board.md:124`: "Lacks acceptance criteria by design... `/jared-stage` exempts these"), matched by `stage.py`'s `is_epic()` check (checked at `stage.py:386-387`). No equivalent documented shape exists for 'phase-tracking issue that is a child of an epic' (e.g. #349, #350 per docs/superpowers/specs/2026-06-10-marketplace-readiness-review-design.md:163-164, "one phase issue each ... Phase 0 → #349, Phase 1 → #350"). The canonical `assets/issue-body.md.template` (which `commands/jared-file.md` step 4 instructs every filer to follow, with no phase-issue carve-out) requires `## Acceptance criteria`; the phase issues departed from that template by using a bold…
- **Evidence:** docs/project-board.md:124: "`epic` | Durably parent-shaped ... Lacks acceptance criteria by design; stays in Backlog as a long-horizon container. `/jared-stage` exempts these from 'Deferred (this pass)' noise." skills/jared/scripts/stage.py:184-197 `is_epic()` + line 386-387 `if is_epic(item): # #146: epics are durably parent-shaped and stay in Backlog by [design]`. skills/jared/assets/issue-body.md.template:9 `## Acceptance criteria` (canonical, no phase-issue exception documented). docs/superpowers/specs/2026-06-10-marketplace-readiness-review-design.md:163-164: "tracked ... as epic **#348** on Project #4, with one phase issue each ... Phase 0 → #349, Phase 1 → #350". docs/superpowers/reviews/2026-06-10-marketplace-readiness-ledger.md:21: "`/jared-start` | ◑ (used live to start #349 — see F2)" (i.e. it was started successfully, not blocked) and line 124 (F2 row itself: "body has no…
- **Suggested fix:** Write the convention down explicitly, choosing one: (a) require epic-child phase-tracking issues to still carry a canonical `## Acceptance criteria` section (content can simply restate the plan's per-phase Deliverables as bullets), and add a phase-issue example to commands/jared-file.md / issue-body.md.template; or (b) introduce a documented, code-recognized exemption for phase issues (e.g. extend is_epic()-style logic or a parallel label) mirroring the existing `epic` label's treatment in…
- **Verification:** 2 refute-first verifiers · 0 refuted · 0 uncertain.

#### F26 · P2 · dim 1c — commands/jared-stage.md documents a stale Up Next cap default (3, should be 8)

- **Location:** `commands/jared-stage.md:108`
- **Claim:** The stub tells the operator the default Up Next cap is 3, but the actual `stage.py` default has been 8 since commit e39ce30 (#245, 'raise default WIP caps to 8 Up Next / 4 In Progress'). SKILL.md was updated by that same commit and correctly says 8 — only this command stub was missed.
- **Evidence:** commands/jared-stage.md:108: "- `--up-next-cap <N>`: override the default Up Next cap of 3. Useful for projects with different WIP norms." vs. `skills/jared/scripts/stage.py` argparse: `parser.add_argument("--up-next-cap", type=int, default=8, help="Maximum items allowed in Up Next column (default: 8).")` (confirmed via `stage.py --help` -> "default: 8"). `git show e39ce30` commit message states "stage.py --up-next-cap default 3 → 8. ... SKILL.md updated to describe the new defaults" (jared-stage.md not mentioned/touched). `skills/jared/SKILL.md:285` correctly says "Up Next caps at 8", confirming jared-stage.md is the outlier.
- **Suggested fix:** Update commands/jared-stage.md:108 to read "override the default Up Next cap of 8".
- **Verification:** 2 refute-first verifiers · 0 refuted · 0 uncertain.

#### F27 · P2 *(verifiers corrected down from P1)* · dim 1c — Hand-written 'degraded:' examples in SKILL.md/operations.md don't follow the phrasing they claim is canonical

- **Location:** `skills/jared/SKILL.md:204,230,291,293,299,310; skills/jared/references/operations.md:4,5,6,7,8`
- **Claim:** Both docs state the single canonical degradation phrasing is `degraded: <feature> unavailable on <backend> — <instead>` (SKILL.md:26, operations.md:14), matching what `capabilities.py::degraded_note()` actually emits (`f"degraded: {feature} unavailable on {backend} — {instead}"`). But 10 of the 11 hand-authored prose examples that follow this claim do not match that template — most omit the `on <backend>` segment entirely, and one uses an entirely different shape.
- **Evidence:** Canonical form, skills/jared/scripts/lib/capabilities.py:37: `return f"degraded: {feature} unavailable on {backend} — {instead}"`. Doc's own restatement, SKILL.md:26: "a one-line `degraded: <feature> unavailable on kanbanflow — <instead>` note"; operations.md:14: "`degraded: <feature> unavailable on <backend> — <instead>`". Non-conforming examples (missing ` on <backend>` between 'unavailable' and the em-dash): SKILL.md:204 `degraded: CLOSED_STATE unavailable — auto-move polling is GitHub Projects v2-specific...`; SKILL.md:230 `degraded: MILESTONE_STATE unavailable — --type milestones audit and date-based milestone triggers do not apply`; SKILL.md:291 `degraded: VELOCITY_TIMESTAMPS unavailable — aging checks not available on this backend; all items appear fresh`; SKILL.md:293 `degraded: VELOCITY_TIMESTAMPS unavailable — cycle time not captured on this backend`; SKILL.md:299 `degraded:…
- **Suggested fix:** Rewrite each non-conforming example to literally follow `degraded: <feature> unavailable on <backend> — <instead>` (using the actual backend name, e.g. 'kanbanflow', and a single feature phrase rather than joining two capabilities with '/' or 'and').
- **Verification:** 2 refute-first verifiers · 0 refuted · 0 uncertain.

#### F28 · P2 · dim 1c — plan-spec-integration.md claims archive-plan.py commits its changes; the script has no git-commit code at all

- **Location:** `skills/jared/references/plan-spec-integration.md:127`
- **Claim:** The doc lists the archival script's steps and ends with "6. Commits with a standardized message." `archive-plan.py` never calls git commit — it only moves the plan file, prepends a header, and PATCHes the issue body via `gh`/GraphQL. Any actual commit happens later, generically, as part of `/jared-wrap`'s broader commit→push→PR flow (not a standardized archival-specific message).
- **Evidence:** Quoted block (plan-spec-integration.md:121-127): "The script: 1. Parses the `## Issue` section. 2. Queries each referenced issue's state. 3. If all closed, moves the file to `archived/YYYY-MM/`... 4. Prepends the archival header. 5. Updates the issue's `## Planning` section... 6. Commits with a standardized message." `grep -in commit skills/jared/scripts/archive-plan.py` returns zero matches, and the file has no `subprocess`/git import at all (only `argparse`, `datetime`, `sys`, `tempfile`, `pathlib`, plus `lib.board` gh helpers). `commands/jared-wrap.md` (the caller) runs `archive-plan.py --scan` as one step inside a generic session-wide "commit → push → PR create" back-end flow (step 5b) — there is no archival-specific commit message.
- **Suggested fix:** Drop step 6, or rephrase it as "the calling session commits the archival alongside its other session changes as part of the normal wrap commit" rather than describing it as something the script itself does with a standardized message.
- **Verification:** 2 refute-first verifiers · 0 refuted · 0 uncertain.

#### F29 · P2 · dim 1d — pyyaml>=6.0 is a dead runtime dependency — confirmed, scope is a one-line removal

- **Location:** `pyproject.toml:7`
- **Claim:** `pyyaml>=6.0` is declared under `[project] dependencies` but is imported nowhere in the shipped tree. This matches the review's own already-tracked F1 (docs/superpowers/reviews/2026-06-10-marketplace-readiness-ledger.md:123, status "confirmed → fix in Phase 3"); I independently re-verified it by reading rather than trusting the ledger. Scope of removal is exactly the one dependency-list entry — nothing else in the repo references PyYAML.
- **Evidence:** pyproject.toml:6-8 — `dependencies = [\n "pyyaml>=6.0",\n]`. Repo-wide `grep -rn "import yaml\|from yaml"` across every `.py` file and the extension-less `jared` entry point returns zero hits. The only two files matching a bare `yaml` grep are `skills/jared/scripts/lib/ties.py:360`, where "yaml" is one file-extension alternative inside a regex character class (`r"[A-Za-z0-9_.\-]+\.(?:py|md|ts|tsx|js|jsx|go|rs|java|sh|toml|yaml|yml|json))"`), and `tests/test_integration_item_add_idempotency.py:51`, a comment ("see seed-issues.yaml"). The only other repo file containing the string "requires.txt" (`jared.egg-info/requires.txt`, which does list `pyyaml>=6.0`) is a local, gitignored, untracked pip-egg-info build artifact (`.gitignore:50` — `*.egg-info/`; `git ls-files jared.egg-info/` returns nothing), not shipped.
- **Suggested fix:** Delete the `"pyyaml>=6.0",` line from `[project] dependencies` in pyproject.toml.
- **Verification:** 2 refute-first verifiers · 0 refuted · 0 uncertain.

#### F30 · P2 *(verifiers corrected down from P1)* · dim 1e — wrap-state's PR-status JSON→enum translation has zero test coverage

- **Location:** `skills/jared/scripts/jared:1619-1628 (in _cmd_wrap_state)`
- **Claim:** All three `jared wrap-state` CLI tests (tests/test_cmd_wrap_state.py) pin `"statusCheckRollup": []` in their fake `gh pr view` payload. That specific input short-circuits the aggregation to the `else: checks_status = "none"` branch — meaning the `failed` branch (FAILURE/CANCELLED/TIMED_OUT), the `pending` branch (IN_PROGRESS/PENDING/QUEUED), and the `passed` branch (non-empty rollup with none of the above) are never exercised by any test in the repo. The pure decision function `decide_next_step` IS exhaustively tested in tests/test_wrap_state.py, but those tests hand it an already-computed `checks_status` string directly — they never go through the CLI's own translation from raw `statusCheckRollup` JSON. Same gap applies to the adjacent `mergeable_value == "UNKNOWN"` → `mergeable=None` branch (jared:1610-1616), also untested at the CLI level.
- **Evidence:** skills/jared/scripts/jared:1619-1628: ``` rollup = pr_payload.get("statusCheckRollup") or [] statuses = {c.get("conclusion") or c.get("status") for c in rollup} if "FAILURE" in statuses or "CANCELLED" in statuses or "TIMED_OUT" in statuses: checks_status: str = "failed" elif "IN_PROGRESS" in statuses or "PENDING" in statuses or "QUEUED" in statuses: checks_status = "pending" elif rollup: checks_status = "passed" else: checks_status = "none" ``` All three fake PR payloads in tests/test_cmd_wrap_state.py use `"statusCheckRollup": []` (lines 28-35 uses no PR at all; lines 81 and 103 both literally contain `'"statusCheckRollup": []'`), which only ever reaches the final `else` branch. grep across tests/*.py for `statusCheckRollup` outside test_cmd_wrap_state.py returns nothing, confirming no other test exercises this translation. Jared's own repo also has no CI, so on jared's own board…
- **Suggested fix:** Add 1-2 cases to tests/test_cmd_wrap_state.py using the existing `_patch_pr_view` helper with a non-empty `statusCheckRollup` (e.g. one entry with `conclusion: "FAILURE"` and one with `status: "IN_PROGRESS"`), asserting the CLI prints `surface_failure` / `wait_checks` respectively. Also add a `mergeable: "UNKNOWN"` case.
- **Verification:** 2 refute-first verifiers · 0 refuted · 0 uncertain.

#### F31 · P2 *(verifiers corrected down from P1)* · dim 1e — `jared propose-partition` silently drops the NATIVE_DEPENDENCIES degradation note on KanbanFlow — and no test exercises the degraded path to catch it

- **Location:** `skills/jared/scripts/jared:1664 (_cmd_propose_partition); contrast skills/jared/scripts/jared:1327-1336 (_cmd_ties); skills/jared/scripts/lib/board.py:686-690 (docstring); tests/test_cmd_propose_partition.py (whole file, 82 lines)`
- **Claim:** `_cmd_propose_partition` calls `board.fetch_open_issues_for_ties()` (jared:1664) but, unlike its sibling `_cmd_ties`, never calls `degraded_or_none(...)` afterward to surface the NATIVE_DEPENDENCIES degradation note — even though `fetch_open_issues_for_ties`'s own docstring (board.py:686-690) explicitly claims 'The jared-ties/jared-propose-partition callers emit the degradation note at the diagnostic layer.' `lib/partition.py` has zero references to `degraded`/`Capability` (grepped, confirmed empty), so the note truly never fires. On a KanbanFlow-backed board (which lacks NATIVE_DEPENDENCIES per kanbanflow_provider.py:46), propose-partition silently treats every issue as unblocked (blocked_by=(), board.py:711) with no indication to the operator that dependency data is missing — while `jared ties` on the identical backend does warn. `tests/test_cmd_propose_partition.py` has zero hits…
- **Evidence:** jared:1664 `open_items = board.fetch_open_issues_for_ties()` with no following degraded_or_none call. Sibling at jared:1329-1336: `native_dep_note = degraded_or_none(board, Capability.NATIVE_DEPENDENCIES, "native blocked-by edges for ties", "blocked_by fields will be empty — ties analysis uses body-ref signals only")` then `if native_dep_note: diagnostic = ...`. board.py:686-690 docstring: 'Phase 6: when NATIVE_DEPENDENCIES is absent, skips the GitHub GraphQL path ... The jared-ties/jared-propose-partition callers emit the degradation note at the diagnostic layer.' `grep -n "degraded|Capability|capabilities()" skills/jared/scripts/lib/partition.py` returns nothing. `grep -n "degraded|NATIVE_DEPENDENCIES|restrict_capabilities|kanbanflow" tests/test_cmd_propose_partition.py` returns nothing. docs/superpowers/reviews/2026-06-10-marketplace-readiness-ledger.md:48 marks `propose-partition`…
- **Suggested fix:** Add a `degraded_or_none(board, Capability.NATIVE_DEPENDENCIES, "native blocked-by edges for partition proposals", "blocked_by-aware sequencing signals will be unavailable")` call in `_cmd_propose_partition` (mirroring `_cmd_ties`) and print it before/alongside the proposal output; add a CLI-level test in test_cmd_propose_partition.py using `restrict_capabilities` + `import_cli()` that asserts the note appears in stdout on a degraded backend.
- **Verification:** 2 refute-first verifiers · 0 refuted · 0 uncertain.

#### F32 · P2 · dim 1e — propose-partition's existing-session-label parsing (CLI-only logic) is never exercised

- **Location:** `skills/jared/scripts/jared:1665-1674 (in _cmd_propose_partition)`
- **Claim:** The CLI-specific loop that scans each open issue's GitHub labels for `session-N` and builds the `existing_labels: dict[int, int]` passed into `propose_partition` is never tested with a non-empty label set. Both CLI tests in tests/test_cmd_propose_partition.py pass `labels_by_number={1: [], 2: []}` (all issues label-less), so the `label.startswith("session-")` extraction, the `int(...)` parse with its `except ValueError: continue`, and the `1 <= n <= K` range guard are all dead code from a test-coverage standpoint. `tests/test_partition.py::test_propose_partition_honors_existing_labels` tests the *library* function `propose_partition` by handing it a `dict` directly — it does not touch the CLI's translation from raw label strings to that dict. The `--sessions` < 2 guard (jared:1654-1656) is also untested.
- **Evidence:** skills/jared/scripts/jared:1665-1674: ``` existing_labels: dict[int, int] = {} for item in open_items: for label in item.labels: if label.startswith("session-"): try: n = int(label.removeprefix("session-")) except ValueError: continue if 1 <= n <= K: existing_labels[item.number] = n break ``` tests/test_cmd_propose_partition.py both calls pass `labels_by_number={1: [], 2: []}` (lines 33 and 55) — no test in the file passes a `session-N` label on any issue, so `keep` (pre-existing assignment respected) is never actually exercised through the CLI; only `add` (fresh assignment) is asserted.
- **Suggested fix:** Add a case to tests/test_cmd_propose_partition.py where one issue already carries a `session-1` label and assert it lands in `proposal.keep` (or JSON's `keep` list) rather than `add`. Optionally also cover a malformed label like `session-abc` (hits the ValueError continue) and `jared propose-partition --sessions 1` (should exit 2).
- **Verification:** 2 refute-first verifiers · 0 refuted · 0 uncertain.

#### F33 · P2 · dim 1f — getting-started.md's description of the `/jared-file` interactive flow omits the mandatory milestone question

- **Location:** `docs/getting-started.md:44-46 (actual requirement: skills/jared/scripts/jared:196-225, commands/jared-file.md:59-60)`
- **Claim:** getting-started.md says Jared gathers only 'title, body, priority' when filing the newcomer's very first issue. In reality `jared file` refuses to run (exit 2) unless exactly one of --milestone/--no-milestone is also supplied — a mandatory question the doc never mentions, on a freshly-bootstrapped project that plausibly has zero milestones yet. The underlying command doctrine (commands/jared-file.md) does handle this correctly, so nothing breaks, but the walkthrough undersells what the newcomer will actually be asked on their first-ever `/jared-file` run.
- **Evidence:** docs/getting-started.md:44-46: "Jared will gather the inputs — either by reading what you already described in your message, or by asking one short question at a time (title, body, priority)." vs. commands/jared-file.md:59-60: "Exactly one of `--milestone \"<title>\"` or `--no-milestone` is mandatory — `jared file` exits 2 without one (#238)." and skills/jared/scripts/jared:210-214 comment confirming enforcement lives in `_cmd_file`.
- **Suggested fix:** Add milestone to the list of inputs getting-started.md says will be gathered, and note that a brand-new project with no milestones yet should answer 'no milestone'.
- **Verification:** 2 refute-first verifiers · 0 refuted · 0 uncertain.

#### F43 · P2 · dim 1a — --show silently discards --current-state / --decision when passed together, exiting 0 with no indication the mutation was skipped

- **Location:** `skills/jared/scripts/capture-context.py:227-234 (the --show branch) vs. :243-246 (the mutation logic it precedes)`
- **Claim:** main() checks `if args.show:` first (line 227), prints the current sections, and returns 0 (line 234) — before the code ever inspects `args.current_state` or `args.decision` (lines 243-246). If an operator invokes the script with both `--show` and `--current-state "..."` (or `--decision "..."`) — plausible from a copy-pasted command line where a leftover `--show` wasn't removed — the tool prints only the existing state and exits successfully, silently ignoring the requested update. No warning or error names the conflicting flags; the exit code and printed output give no signal that a mutation was requested and skipped.
- **Evidence:** Lines 227-234: `if args.show: print(...) ... return 0` runs unconditionally before any check of `args.current_state`/`args.decision`. The docstring's usage examples (lines 8-21) never show --show combined with a mutation flag, so this combination is undocumented and unguarded.
- **Suggested fix:** Reject --show combined with --current-state/--decision at argument-parsing time (argparse mutually-exclusive group), or print an explicit note when --show suppresses a requested mutation.
- **Verification:** 2 refute-first verifiers · 0 refuted · 0 uncertain.

#### F44 · P2 · dim 1a — Module docstring's 'Preserves all other sections exactly' is false — reassemble silently reorders custom sections relative to known ones

- **Location:** `skills/jared/scripts/capture-context.py:4 (claim) vs. :173-198 (reassemble implementation)`
- **Claim:** Line 4 of the module docstring states 'Preserves all other sections exactly,' but reassemble() does not preserve section order: it places every SECTION_ORDER member first in that fixed sequence (lines 180-185), then appends any remaining (unknown/custom) sections afterward in their original relative order (lines 187-194). Any issue body whose H2 headings interleave a custom section between two SECTION_ORDER sections (e.g. `## Current state / ## Notes / ## Acceptance criteria`) comes back reordered to `## Current state / ## Acceptance criteria / ## Notes` on a run invoked only to touch Current state — a section the operator never asked to move gets moved. The test at lines 103-135 of tests/test_capture_context.py explicitly asserts this reordering is intentional behavior ('Current state must come before Depends on per SECTION_ORDER, despite appearing after it in the input'), which…
- **Evidence:** Line 4: `Preserves all other sections exactly.` Lines 180-185: `for name in SECTION_ORDER: if name in sections: out_parts.append(sections[name]) ... placed.add(name)`. Test file lines 123-127: `# Current state must come before Depends on per SECTION_ORDER, despite appearing after it in the input.`
- **Suggested fix:** Either fix the docstring to state that known sections are reordered to SECTION_ORDER while only unlisted sections keep their relative order, or change reassemble to preserve the original heading order entirely and only rewrite the targeted section's body in place.
- **Verification:** 2 refute-first verifiers · 0 refuted · 0 uncertain.

#### F45 · P2 · dim 1a — split_sections silently drops content when a body has two identically-named H2 headings

- **Location:** `skills/jared/scripts/capture-context.py:99-129 (split_sections), interacting with :173-198 (reassemble)`
- **Claim:** split_sections stores each section under `sections[current_name] = ...` keyed only by heading text (line 113 for a mid-stream close, line 124 for end-of-file close). If a body contains two H2 headings with the same title (e.g. two `## Decisions` blocks from a manual edit or copy-paste), the second occurrence silently overwrites the dict entry for the first — the first occurrence's content is discarded with no warning, no merge, and no diff attribution back to 'lost content that was never in the new body to begin with' (the diff only shows old-body-vs-new-body, and old-body already contained the duplicate, so a human skimming the diff sees content disappear but has no signal it was a parsing artifact rather than an intentional replacement). This is narrow in practice — jared's own template and append_decision/update_current_state never themselves create a second '## Decisions' or '##…
- **Evidence:** Line 113: `sections[current_name] = "".join(current_lines)` (executes again for a second same-named heading, overwriting the first's stored content). Line 124: identical overwrite at end-of-file close. No dedup or duplicate-detection anywhere in split_sections or reassemble.
- **Suggested fix:** Detect duplicate heading names during split_sections and either raise a clear error ('duplicate ## Decisions heading — refusing to guess which one to edit') or concatenate/merge the bodies instead of silently keeping only the last one.
- **Verification:** 2 refute-first verifiers · 0 refuted · 0 uncertain.

#### F46 · P2 · dim 1a — _cmd_close prints two different "OK:" success lines for one close operation

- **Location:** `skills/jared/scripts/jared:544 and :555 (_cmd_close)`
- **Claim:** One close operation emits both "OK: set Status=Done on issue #{N}" and "OK: closed #{N}, Status=Done{suffix}". tests/test_cmd_close.py:280 asserts only the second, so the first is unpinned and looks like unswept leftover from the Phase-6 degradation-suffix work rather than intentional output.
- **Evidence:** skills/jared/scripts/jared:544, :555 both print an OK: line in the same success path; tests/test_cmd_close.py:280 asserts only the latter.
- **Suggested fix:** Decide whether the double line is intentional; if not, drop the first and pin the remaining line in the test.
- **Verification:** 2 refute-first verifiers · 1 refuted · 0 uncertain.
- **Source:** surfaced by the completeness critic, not a finder — then held to the same refute-first bar.
- **Weak survivor:** 1 of 2 verifiers refuted this. It survives under the plan's "keep iff fewer than a majority refute" rule, but it is among the least certain entries — re-check before acting.

#### F47 · P2 · dim 1c — docs/bake-sites.md cites a non-existent asset filename (missing .template suffix)

- **Location:** `docs/bake-sites.md:29`
- **Claim:** The line reads "populated per `skills/jared/assets/project-board.md`", but no such file exists — the real asset is skills/jared/assets/project-board.md.template. A reader following the citation finds nothing.
- **Evidence:** Stage-0 backtick sweep flagged docs/bake-sites.md:29 -> skills/jared/assets/project-board.md as unresolvable; `ls skills/jared/assets/` shows project-board.md.template, not project-board.md.
- **Suggested fix:** Add the .template suffix.
- **Verification:** 2 refute-first verifiers · 0 refuted · 0 uncertain.
- **Source:** surfaced by the completeness critic, not a finder — then held to the same refute-first bar.

#### F48 · P2 · dim 1c — Stated '~10 graphql points per project item' rate contradicts the doc's own replicated primary measurement

- **Location:** `docs/github-api-tool-selection.md:11 (TL;DR) vs. lines 121 and 239`
- **Claim:** The doc asserts a single linear cost model — '~10 graphql points per project item' — for `gh project item-list`, but its own best-replicated data point (jared/projects/4, called out elsewhere as 'consistent across windows') is 203 points for ~50 items, roughly 4 points/item, not 10. Only the far-less-verified findajob data point (a single unreplicated ~5000/~500-item observation) matches the stated ~10/item rate.
- **Evidence:** Line 11: 'Cost scales linearly with item count (~10 graphql points per project item...). On `jared/projects/4` (~50 items) one call costs ~200 points.' 200/50 = ~4/item, not 10/item, per the doc's own arithmetic. Line 121 (per-call cost table): '**~10 points per project item.** 203 measured on jared/projects/4 (~50 items)...' — again 203/50 ≈ 4.06. Line 239 (validation log): '`gh project item-list 4 --owner brockamer --limit 2000 --format json` | 1× clean + 1× pre-quiet | OK | +203 graphql (consistent across windows)' — the doc itself flags this as the more-trustworthy, replicated figure, yet the headline per-item rate is calibrated to the single unreplicated 500-item observation instead.
- **Suggested fix:** Either present the per-item rate as a range bounded by both observations (~4-10/item) or note explicitly that per-item cost likely depends on field-value count per project (which could differ between jared's and findajob's boards) rather than asserting one flat rate that its own replicated measurement contradicts.
- **Verification:** 2 refute-first verifiers · 0 refuted · 0 uncertain.

#### F49 · P2 *(verifiers corrected down from P1)* · dim 1c — Doc still describes the issue-body REST migration as proposed/pending though it shipped

- **Location:** `docs/github-api-tool-selection.md:15,80,185,187 (vs. its own line 256; skills/jared/scripts/capture-context.py:79; skills/jared/scripts/lib/github_provider.py:407-427)`
- **Claim:** Four passages in the doc present the capture-context.py/archive-plan.py issue-body REST migration as a not-yet-done recommendation ('migrate from...', 'rec — proposed migration...', 'currently use gh issue view N --json body', 'Filed as a follow-up issue... investigation only'), but the doc's own 'What shipped' section (line 256) says this migration already landed via PR #212, and the current code confirms it: capture-context.py no longer calls `gh issue view --json body` at all.
- **Evidence:** Line 15: '→ migrate from `gh issue view --json body` to `gh api repos/.../issues/N`.' Line 80: '(rec — proposed migration for `capture-context.py`, `archive-plan.py`)'. Line 185: '`capture-context.py` and `archive-plan.py` currently use `gh issue view N --json body`'. Line 187: 'Filed as a follow-up issue rather than this PR's scope (per AC: investigation only).' Contradicted by line 256: '2. **Batch-script migration** — `capture-context.py` + `archive-plan.py` issue-body reads moved to REST (#208, PR #212).' Confirmed in code: `capture-context.py:79` is `return cast("str", _make_provider(repo).get_body(number))`, and `github_provider.py:425` implements `get_body` as `data = _retry_transient_read(lambda: run_gh(["api", f"repos/{self.repo}/issues/{ref}"]))` — a direct REST call, not `gh issue view --json body`. `grep -n "issue view" skills/jared/scripts/capture-context.py…
- **Suggested fix:** Update lines 15, 80, 185, 187 to reflect that the migration shipped (PR #208/#212), pointing at the current `get_body()`/`fetch_issue_state_rest()` REST implementations instead of describing the old `gh issue view --json body` behavior as current.
- **Verification:** 2 refute-first verifiers · 0 refuted · 0 uncertain.

#### F50 · P2 · dim 1c — 'Open questions' MCP-scope-diagnostic item is stale — the doc's own 'What shipped' section says it's done

- **Location:** `docs/github-api-tool-selection.md:225 (vs. its own line 258; skills/jared/scripts/lib/board.py:1034-1078)`
- **Claim:** The 'Open questions' section still poses the MCP-scope-diagnostic gap as unresolved ('the diagnostic won't fire... Likely a one-line addition'), but the doc's own 'What shipped' section says this shipped, and the current code confirms `_format_token_scope_diagnostic()` already always appends an MCP note regardless of which mechanism failed.
- **Evidence:** Line 225: '`_format_token_scope_diagnostic` in `board.py` only knows about `gh`'s token. If a future MCP-routed mutation fails with the same... signature, the diagnostic won't fire.' Contradicted by line 258: '4. **MCP scope diagnostic** — `_format_token_scope_diagnostic` extended for MCP-token scope failures (#210, PR #213).' Confirmed in code: `board.py`'s `_format_token_scope_diagnostic()` docstring (line ~1034) states 'Always appends an MCP note (#210)... This function is only ever reached from `run_gh_raw`'s failure path... The line is therefore not a detection signal but a UX hint,' and the function body unconditionally appends a 'MCP note:' paragraph.
- **Suggested fix:** Delete or mark resolved the 'MCP scope diagnostic' bullet under Open Questions, pointing at the shipped `_format_token_scope_diagnostic()` behavior instead.
- **Verification:** 2 refute-first verifiers · 0 refuted · 0 uncertain.

#### F51 · P2 *(verifiers corrected down from P1)* · dim 1c — board-sweep.md's example dependency-graph.py invocation omits the required --repo flag, so it fails as written

- **Location:** `skills/jared/references/board-sweep.md:102 vs skills/jared/scripts/dependency-graph.py:382`
- **Claim:** board-sweep.md tells the groomer to 'Run `scripts/dependency-graph.py --summary` for a compact view,' but dependency-graph.py declares `--repo` as `required=True` with no default and no fallback to `docs/project-board.md` (confirmed by reading main()'s full argparse block and the body of main(), which immediately calls `fetch_open_issues(args.repo, ...)`). Run exactly as the doc shows, argparse rejects the invocation for the missing required argument before any work happens. Every other reference doc that cites this same script (e.g. dependencies.md: '${CLAUDE_PLUGIN_ROOT}/skills/jared/scripts/dependency-graph.py --repo <owner>/<repo>') includes `--repo <owner>/<repo>`, confirming this is a real omission in board-sweep.md rather than house style.
- **Evidence:** board-sweep.md:102: "Run `scripts/dependency-graph.py --summary` for a compact view." | dependency-graph.py:382: `parser.add_argument("--repo", required=True, help="Repo slug (owner/repo)")` — no default. | dependencies.md's own worked example: `${CLAUDE_PLUGIN_ROOT}/skills/jared/scripts/dependency-graph.py --repo <owner>/<repo>` always supplies --repo.
- **Suggested fix:** Change board-sweep.md:102 to `Run scripts/dependency-graph.py --repo <owner>/<repo> --summary for a compact view.`
- **Verification:** 2 refute-first verifiers · 0 refuted · 0 uncertain.

#### F52 · P2 · dim 1c — context-capture.md and board-sweep.md invoke batch scripts by a bare `scripts/...` path that won't resolve from a consuming project's working directory

- **Location:** `skills/jared/references/context-capture.md:65,71,74,77 and skills/jared/references/board-sweep.md:33,102`
- **Claim:** Jared is a user-scope plugin whose files live under `${CLAUDE_PLUGIN_ROOT}` (installed into `~/.claude/plugins/cache/...`), and the project's own CLAUDE.md states the rule plainly: 'Use `${CLAUDE_PLUGIN_ROOT}/skills/jared/scripts/jared <subcommand>`. Never hardcode `~/.claude/skills/...` paths.' context-capture.md's four capture-context.py invocations (lines 65, 71, 74, 77) and board-sweep.md's two script mentions (lines 33, 102) all use a bare `scripts/<name>.py` path instead. Copy-pasted literally from a target project's directory (the normal case — Jared runs against other repos, not against jared's own tree), `scripts/capture-context.py` does not exist relative to that cwd and the shell reports 'No such file or directory.' Sibling docs in the same reference set get this right (session-continuity.md:123 and commands/jared-wrap.md:76 both spell out…
- **Evidence:** context-capture.md:71: "scripts/capture-context.py --issue <N> --repo <owner>/<repo> --current-state \"...\"" (no plugin-root prefix) | board-sweep.md:102: "Run `scripts/dependency-graph.py --summary`" | CLAUDE.md: "Use `${CLAUDE_PLUGIN_ROOT}/skills/jared/scripts/jared <subcommand>`. Never hardcode `~/.claude/skills/...` paths" | session-continuity.md:123 correctly uses "${CLAUDE_PLUGIN_ROOT}/skills/jared/scripts/capture-context.py --issue <N> --repo <owner>/<repo> --current-state \"...\"".
- **Suggested fix:** Prefix every bare `scripts/*.py` invocation in context-capture.md and board-sweep.md with `${CLAUDE_PLUGIN_ROOT}/skills/jared/`, matching session-continuity.md and the command stubs.
- **Verification:** 2 refute-first verifiers · 0 refuted · 0 uncertain.

#### F53 · P2 · dim 1c — Hand-written `degraded:` example strings don't match the canonical phrasing (two more surviving instances)

- **Location:** `skills/jared/references/new-board.md:56; skills/jared/references/milestones-and-roadmap.md:3`
- **Claim:** new-board.md:56 quotes `degraded: CLOSED_STATE unavailable — Item closed auto-move workflow is GitHub Projects v2-specific; not applicable on this backend`. milestones-and-roadmap.md:3 quotes `degraded: MILESTONE_STATE unavailable on this backend — milestone creation, due dates, Roadmap view, and gh milestone PATCH commands do not apply`.
- **Evidence:** skills/jared/scripts/lib/capabilities.py:32-35 defines the single canonical formula: `degraded_note()` returns `f"degraded: {feature} unavailable on {backend} — {instead}"`, where `feature` is a plain-English phrase and `backend` is the real backend name (e.g. "kanbanflow"). Real call sites never emit the raw enum name: e.g. `_cmd_close`'s CLOSED_STATE gate (jared:548-553) uses feature="native close state" producing "degraded: native close state unavailable on kanbanflow — ...", and the MILESTONE_STATE gate in `_cmd_file` (jared:588-593) uses feature="milestone assignment". Both quoted doc strings instead substitute the raw Capability enum member name (`CLOSED_STATE`/`MILESTONE_STATE` from board_provider.py:21,25) for the feature phrase, and new-board.md's string additionally omits "on <backend>" entirely. Round 1 already filed this defect class for SKILL.md/operations.md/jared-cli.md;…
- **Suggested fix:** Rewrite both example strings to plug a real feature phrase and the actual backend name into the canonical template, e.g. "degraded: native close state unavailable on kanbanflow — no native closed state on this backend" and "degraded: milestone assignment unavailable on kanbanflow — this backend has no milestone state or due dates".
- **Verification:** 2 refute-first verifiers · 0 refuted · 0 uncertain.

#### F54 · P2 · dim 1c — new-board.md repeats the false "jared close polls for auto-move" claim (separate from the already-filed SKILL.md instance)

- **Location:** `skills/jared/references/new-board.md:64`
- **Claim:** The close-paths table states: "`jared close <N>` | No — polls for auto-move, falls back to explicit `Status=Done`".
- **Evidence:** skills/jared/scripts/lib/github_provider.py:878-882 docstring: "Mirrors _cmd_close: optional comment → gh issue close → set_field (always, per #137 defense-in-depth; no poll)." `_cmd_close` in skills/jared/scripts/jared:519-556 unconditionally calls `board.provider.close()` then prints "OK: set Status=Done" — there is no poll/retry loop anywhere in the close path. Round 1 already filed this exact misconception for SKILL.md:206; this is the same defect independently present in new-board.md's own close-paths table.
- **Suggested fix:** Change the row to describe the real mechanism: "No — sets Status=Done directly as defense-in-depth after `gh issue close` (no polling)". The table's bottom-line conclusion (safe either way, no workflow required) is correct; only the mechanism description is wrong.
- **Verification:** 2 refute-first verifiers · 0 refuted · 0 uncertain.

#### F55 · P2 *(verifiers corrected down from P1)* · dim 1c — pii-pre-flight.md lists two pre-flight consumers; the code has three (_cmd_close also calls it)

- **Location:** `skills/jared/references/pii-pre-flight.md:34 vs skills/jared/scripts/jared:525`
- **Claim:** The doc says "`jared file` and `jared comment` both run the pre-flight immediately after resolving the body", naming two consumers. pre_flight_check is also called from _cmd_close at jared:525, guarding the optional --body/--body-file comment-before-close path. The consumer list is stale, and this is the security-relevant redaction gate.
- **Evidence:** pii-pre-flight.md:34 names only file and comment; skills/jared/scripts/jared:525 contains a third pre_flight_check call site inside _cmd_close.
- **Suggested fix:** Add `jared close --body/--body-file` to the documented consumer list.
- **Verification:** 2 refute-first verifiers · 0 refuted · 0 uncertain.
- **Source:** surfaced by the completeness critic, not a finder — then held to the same refute-first bar.

#### F56 · P2 · dim 1c — session-continuity.md's 'iff board has Session start checks configured' claim omits the required --include-session-checks flag

- **Location:** `skills/jared/references/session-continuity.md:175 (vs. skills/jared/scripts/jared:271-278, 1270)`
- **Claim:** The doc states the Quick health check block appears in `jared next-session-prompt`'s output 'iff the board has `## Session start checks` configured' — but the gate is actually a two-part AND: the caller must also pass `--include-session-checks`. Running `jared next-session-prompt` bare (as the doc itself writes the command at line 175, with no flags) will not print the block even when the board has the section configured.
- **Evidence:** session-continuity.md:175: '`${CLAUDE_PLUGIN_ROOT}/skills/jared/scripts/jared next-session-prompt` emits the board-derived posture deterministically: ... and a `## Quick health check` block iff the board has `## Session start checks` configured.' The CLI defines `--include-session-checks` as `action="store_true"` (default False) at jared:271-278, and the emission is gated by `if args.include_session_checks and board.session_start_checks:` at jared:1270 — both conditions are required, not just the board config.
- **Suggested fix:** State the actual gate: 'iff the board has Session start checks configured AND the caller passes --include-session-checks (which /jared-start always does)'.
- **Verification:** 2 refute-first verifiers · 0 refuted · 0 uncertain.

#### F57 · P2 · dim 1c — structural-review.md's backend-gate degraded: strings omit the canonical 'on <backend>' clause

- **Location:** `skills/jared/references/structural-review.md:4-6 (vs. skills/jared/scripts/lib/capabilities.py degraded_note())`
- **Claim:** All three quoted degraded: strings for Q3/Q4/Q6 drop the 'on <backend>' segment that lib/capabilities.py defines as the single canonical phrasing, so none of the three quoted strings match what the codebase's one consistency anchor actually produces.
- **Evidence:** structural-review.md:4: '`degraded: MILESTONE_STATE unavailable — Q3 milestone/Roadmap checks do not apply`'; :5: '`degraded: NATIVE_DEPENDENCIES unavailable — dependency graph is label-emulation only; ...`'; :6: '`degraded: MILESTONE_STATE unavailable — milestone done-state gating is not mechanically enforceable; ...`'. lib/capabilities.py's `degraded_note()` docstring states the single Phase-6 phrasing is `degraded: <feature> unavailable on <backend> — <instead>` (i.e. it must read '...unavailable on kanbanflow — ...'); all three quoted strings in structural-review.md omit 'on kanbanflow' entirely.
- **Suggested fix:** Insert 'on kanbanflow' into all three quoted strings so they read e.g. `degraded: MILESTONE_STATE unavailable on kanbanflow — Q3 milestone/Roadmap checks do not apply`.
- **Verification:** 2 refute-first verifiers · 0 refuted · 0 uncertain.

#### F58 · P2 · dim 1c — voice.md's boundary table misattributes 'drift-reconcile prompts' to /jared-groom

- **Location:** `skills/jared/references/voice.md:16 (vs. commands/jared-groom.md and skills/jared/SKILL.md:9-13)`
- **Claim:** voice.md's on/off table says 'Drift-reconcile prompts in `/jared-groom`' are ON. But the scenario voice.md itself illustrates under that heading (Situation 2, lines 130-142: 'the user's about to make a large change and there's no issue tracking it') is not part of /jared-groom's routine sweep at all — it's the general, command-independent skill trigger described in SKILL.md's frontmatter ('when Claude is about to modify 3+ files without an open issue'). commands/jared-groom.md's actual content (metadata, WIP, aging, dependency, plan/spec drift, label hygiene sweep) contains no such 'no issue exists for this work' prompt.
- **Evidence:** voice.md:16: '| Drift-reconcile prompts in `/jared-groom` | ON (apologetic-but-resolute about operational integrity) |'. voice.md:130-142 (Situation 2) frames this as a mid-session interrupt ('the user's about to make changes to four files and there isn't an issue representing the work'), matching SKILL.md's frontmatter trigger list (SKILL.md:11-13: '... or when Claude is about to modify 3+ files without an open issue, prepare a PR, close out a feature, or write a plan/spec'), not anything in commands/jared-groom.md, whose full flow (read) covers only the sweep.py-driven metadata/WIP/aging/dependency/label/plan-drift checklist with no such prompt.
- **Suggested fix:** Relabel the table row to name the actual trigger (a general session-level 'about to act without a tracking issue' event) rather than attributing it to the /jared-groom slash command specifically.
- **Verification:** 2 refute-first verifiers · 0 refuted · 0 uncertain.

#### F59 · P2 · dim 1c — voice.md's voice-OFF batch-script list is incomplete relative to SKILL.md's own CLI-string policy

- **Location:** `skills/jared/references/voice.md:24 (vs. skills/jared/SKILL.md:38)`
- **Claim:** voice.md's boundary table lists only three batch scripts ('`bootstrap-project.py`, `archive-plan.py`, `sweep.py`') as voice-OFF operator-facing diagnostics, omitting `capture-context.py`, which SKILL.md's own CLI-string policy explicitly includes in the same voice-OFF list, and which does emit operator-facing stdout diagnostics.
- **Evidence:** voice.md:24: '| `bootstrap-project.py`, `archive-plan.py`, `sweep.py` operator-facing diagnostics | OFF — operator output, not dialogue |' omits `capture-context.py`. SKILL.md:38 ('CLI-string policy: CLI error messages and the operator-facing diagnostics emitted by batch scripts (`sweep.py`, `bootstrap-project.py`, `archive-plan.py`, `capture-context.py`) stay voice-OFF') names all four. capture-context.py does emit such diagnostics, e.g. skills/jared/scripts/capture-context.py:251 `print("(no changes — already up to date)")` and :276 `print(f"Updated #{args.issue}.")`.
- **Suggested fix:** Add capture-context.py to voice.md's boundary-table row so the two doctrine surfaces (SKILL.md and its 'depth' reference voice.md) enumerate the same script list.
- **Verification:** 2 refute-first verifiers · 0 refuted · 0 uncertain.

