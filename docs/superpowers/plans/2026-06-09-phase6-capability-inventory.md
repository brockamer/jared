# Phase 6 capability-surface inventory (completeness proof)

> Generated 2026-06-09 from the `phase6-design-resolution` workflow (four-region fan-out, sonnet). This is the exhaustive enumeration the spec's acceptance criterion #1 requires ("a grep/inventory proves no capability-assuming surface was missed"). **84 surfaces / 24 files / 47 misleading-if-shown.** `SUB_ISSUES` rows are deliberate non-findings (phantom capability — see plan Task 0 / spec Resolved decision 4). The implementation plan ([2026-06-09-phase6-capability-degradation.md](2026-06-09-phase6-capability-degradation.md)) groups these into tasks; each row's `degrade_to` is the per-surface spec.

## CLOSED_STATE — 13 surface(s), 5 misleading

| Surface | File:line | Mislead | Degrade to |
|---|---|:--:|---|
| _cmd_close | `skills/jared/scripts/jared:~481` | — | Behavior is functionally correct — task moves to Done. Degrade by noting in output that close is column-move only (no native closed state); no hard skip needed. |
| _detect_stuck_closed_recent | `skills/jared/scripts/jared:~943` | — | Skip the stuck-closed check with a one-line note: 'degraded: stuck-closed check unavailable on kanbanflow — CLOSED_STATE not supported'. Do not silently omit. |
| dependency-graph find_orphaned | `skills/jared/scripts/dependency-graph.py:267-282` | yes | Skip the orphaned-dependency check with a note: 'degraded: orphaned-dependency check unavailable on kanbanflow — CLOSED_STATE not supported' |
| jared-wrap back-end PR flow (GitHub-only) | `commands/jared-wrap.md:73-171` | yes | When CLOSED_STATE is absent (KanbanFlow backend), skip step 5b entirely: `degraded: closed-state/PR flow unavailable on kanbanflow — back-end PR loop omitted; commit/push manually`. The lock-clear and worktree-remove bullets still apply when sessions used worktrees. Session notes (step 5) and reconciliation still apply. |
| jared-groom 'Closed ≠ Done' sweep check | `commands/jared-groom.md:38-41` | — | When CLOSED_STATE is absent, skip the 'Closed ≠ Done' section entirely with `degraded: closed-state unavailable on kanbanflow — Done column is the sole closed signal; no separate closed-state check needed`. |
| jared-audit milestone close/dissolve via gh api DELETE | `commands/jared-audit.md:104-106` | — | Covered by the MILESTONE_STATE degradation above — if MILESTONE_STATE is absent, milestone mutations are already skipped. This surface is redundant with the milestone degradation but worth calling out explicitly for completeness. |
| board-sweep.md §1 'Stuck closed' — CLOSED_STATE divergence detector | `skills/jared/references/board-sweep.md:L24-26` | yes | On KanbanFlow, there is no separate GitHub closed state — issues are 'closed' only by being in the Done column. The Stuck closed divergence detector is meaningless and should be omitted. Skip-with-note: 'CLOSED_STATE unavailable — Stuck closed check does not apply to this backend; Done column is the only closed signal.' |
| board-sweep.md §1 Item-level semantics — 'open means Project status, not GitHub state' | `skills/jared/references/board-sweep.md:L171-175` | — | On KanbanFlow, there is only one signal (column/status). The two-signal divergence model does not apply. Skip-with-note: 'CLOSED_STATE unavailable — single-signal backend; Stuck closed divergence and open-vs-closed item semantics note does not apply.' |
| new-board.md Auto-move workflow dependency | `skills/jared/references/new-board.md:L53-67` | yes | On KanbanFlow, the GitHub auto-move workflow does not exist; the Done column IS the closed state. Skip-with-note: 'CLOSED_STATE unavailable — Item closed auto-move workflow is GitHub Projects v2-specific; not applicable on this backend.' |
| jared-cli.md close subcommand — polls for auto-move to Done | `skills/jared/references/jared-cli.md:L111-148` | — | On KanbanFlow, there is no auto-move polling (Done column is the sole signal). Skip-with-note: 'CLOSED_STATE unavailable — auto-move polling is GitHub-specific; jared close sets Done directly without polling on this backend.' |
| SKILL.md 'close and verify' — polls for board auto-move | `skills/jared/SKILL.md:L186` | — | On KanbanFlow, auto-move poll is not applicable. Skip-with-note: 'CLOSED_STATE unavailable — auto-move polling is GitHub Projects v2-specific; close sets Done directly on this backend.' |
| SKILL.md frontmatter description — asserts GitHub Projects v2 as the universal backend | `skills/jared/SKILL.md:L3` | yes | On KanbanFlow, the skill operates on a non-GitHub backend. The self-description should be qualified or the capability-check should adjust behavior. Skip-with-note at start of session: 'Backend is KanbanFlow — CLOSED_STATE, MILESTONE_STATE, MCP_TIER, NATIVE_DEPENDENCIES capabilities unavailable.' |
| SKILL.md bootstrap description — 'Confirm which GitHub project' | `skills/jared/SKILL.md:L291` | — | On KanbanFlow, no GitHub project URL is needed. The bootstrap flow should branch on backend type. Skip-with-note: 'CLOSED_STATE / GitHub Projects v2 backend not applicable — bootstrap runs in KanbanFlow mode.' |

## MARKDOWN_BODY — 8 surface(s), 0 misleading

| Surface | File:line | Mislead | Degrade to |
|---|---|:--:|---|
| _cmd_comment — markdown body | `skills/jared/scripts/jared:~762` | — | When MARKDOWN_BODY absent, note in output that comment is stored as plain text. Body content is still stored; no skip needed. Optionally strip markdown formatting before posting. |
| _cmd_file — markdown issue body template | `skills/jared/scripts/jared:~595` | — | When MARKDOWN_BODY absent, note that body is stored as plain text. File operation still proceeds; no skip needed. |
| jared-file issue body template with markdown structure | `commands/jared-file.md:26-37` | — | When MARKDOWN_BODY is absent, skip `<details>` folds and `##` section headers — fall back to plain-text structure (e.g., numbered sections with dashes). Emit `degraded: markdown body rendering unavailable on kanbanflow — using plain-text body structure`. The content is preserved; only the markup changes. |
| jared-start pullable check on markdown-structured body | `commands/jared-start.md:87-91` | — | When MARKDOWN_BODY is absent, the pullable check cannot rely on `##` section headers. Fall back to reading the full plain-text body for an acceptance criteria block. Emit `degraded: markdown body rendering unavailable on kanbanflow — pullable check parses plain text`. The check is still meaningful but the parsing heuristic changes. |
| jared-wrap Session note markdown template | `commands/jared-wrap.md:17-24` | — | When MARKDOWN_BODY is absent, use a plain-text Session note format (section names as plain-text labels rather than markdown headers). Emit `degraded: markdown body rendering unavailable on kanbanflow — session notes use plain-text format`. |
| human-readable-board.md — issue body template with markdown sections and details blocks | `skills/jared/references/human-readable-board.md:L38-64` | — | On KanbanFlow, issue bodies are plain text; `<details>` HTML is not rendered, `##` headings are literal text. Skip-with-note: 'MARKDOWN_BODY unavailable — <details> blocks and markdown heading rendering not available on this backend. Use plain text for body content.' |
| human-readable-board.md — Constraints: no headings deeper than ## | `skills/jared/references/human-readable-board.md:L79-83` | — | On KanbanFlow, markdown heading syntax is not rendered. Constraint becomes moot; plain-text structure should be used instead. Skip-with-note: 'MARKDOWN_BODY unavailable — heading depth constraints do not apply; use plain text.' |
| SKILL.md Human-readable board surface section | `skills/jared/SKILL.md:L276-284` | — | On KanbanFlow, markdown rendering of ## headings and <details> is unavailable. Skip-with-note: 'MARKDOWN_BODY unavailable — structured markdown sections and <details> blocks are GitHub-only. Use plain-text sections on this backend.' |

## MCP_TIER — 7 surface(s), 7 misleading

| Surface | File:line | Mislead | Degrade to |
|---|---|:--:|---|
| _format_token_scope_diagnostic — MCP note in scope error | `skills/jared/scripts/lib/board.py:~986` | yes | When MCP_TIER absent (i.e., not a GitHub backend), omit the MCP-specific paragraph from the scope diagnostic. The remainder of the diagnostic is still valid. |
| SKILL.md and operations.md three-tier MCP-first model (referenced by jared-start, jared-file, jared-groom, jared-reshape) | `commands/jared-start.md:99-116` | yes | When MCP_TIER is absent, skip Tier 1 MCP tool suggestions for board operations. All issue/board ops go through `jared` CLI (Tier 2) or KanbanFlow API directly. Emit `degraded: MCP tier unavailable on kanbanflow — GitHub MCP plugin not applicable; use jared CLI for all board operations`. This affects jared-start step 5 (load context) and any command that recommends MCP tools for rate-limit savings. |
| MCP-first three-tier model (Tier 1 preference) | `skills/jared/SKILL.md:L94` | yes | On a non-GitHub backend (KanbanFlow), the GitHub MCP tools are unavailable by definition; the Tier 1 preference should be scoped to GitHub-backed projects only. Skip-with-note: 'MCP_TIER not available on this backend — skip to Tier 2 (jared CLI) or Tier 3 (gh).' |
| MCP routing when graphql is pressured | `skills/jared/SKILL.md:L96` | yes | GraphQL budget concept does not apply to KanbanFlow backend. Skip-with-note: 'MCP_TIER / GraphQL budget unavailable on this backend.' |
| three-tier model — Tier 1 MCP preference | `skills/jared/references/operations.md:L68-90` | yes | On KanbanFlow, all GitHub-specific mechanisms (gh, gh api graphql, MCP) are inapplicable. Skip-with-note: 'MCP_TIER and GitHub API tiers unavailable — operations route through KanbanFlow provider only.' |
| MCP equivalents table | `skills/jared/references/operations.md:L164-178` | yes | Entire table is inapplicable on KanbanFlow. Skip-with-note: 'MCP_TIER not available on this backend — MCP equivalents table is GitHub-only.' An operator reading this table and trying to follow it on KanbanFlow would attempt gh and MCP calls that fail. |
| Cache discipline / GraphQL budget rules | `skills/jared/references/operations.md:L26-67` | yes | gh / GraphQL budget concepts do not apply on KanbanFlow. Skip-with-note: 'MCP_TIER and GraphQL budget not available on this backend — cache-discipline rules are GitHub-only.' An operator reading and following these rules on KanbanFlow would incorrectly believe they need to manage a GraphQL budget. |

## MILESTONE_STATE — 17 surface(s), 8 misleading

| Surface | File:line | Mislead | Degrade to |
|---|---|:--:|---|
| _cmd_file — milestone error recovery message | `skills/jared/scripts/jared:~571` | yes | When MILESTONE_STATE absent or backend is not GitHub, omit the GitHub-specific recovery command and note that milestones are not supported on this backend. |
| fetch_audit_window — milestones REST query | `skills/jared/scripts/lib/board.py:~1902` | yes | Gate on MILESTONE_STATE; when absent skip the milestone section of audit window and emit a note. Use provider.list_milestones() if a reduced milestones view (swimlanes, no state/due) is wanted. |
| stage milestone_proximity_days ranking key | `skills/jared/scripts/stage.py:83-95 (used in rank_key at line 383)` | — | Already degrades safely to math.inf / '(no milestone)' — incomplete but not misleading. Still benefits from a capability note so the operator knows milestone-proximity ranking is disabled. |
| stage _format_milestone output line | `skills/jared/scripts/stage.py:258-278 (used at line 302)` | — | Already degrades to honest '(no milestone)' text. When MILESTONE_STATE absent, emit a header note: 'degraded: milestone due-date ranking unavailable on kanbanflow — all items sort as no-milestone' |
| jared-reshape milestone inventory + Roadmap question | `commands/jared-reshape.md:13-23` | yes | When MILESTONE_STATE is absent, skip Question 3 entirely with a note: `degraded: milestone dates unavailable on kanbanflow — Roadmap/milestone section omitted`. Omit the Milestones block from the output template. Bundles 2 and 3 (filing/assigning GitHub milestones) are also skipped. |
| jared-audit milestone fetch and date heuristics | `commands/jared-audit.md:13-23` | yes | When MILESTONE_STATE is absent: `--type milestones` and `--type both` are unavailable; checklist question 7 for milestones collapses to 'N/A — no milestone dates on this backend'; step 7 date formula is skipped; step 9 milestone mutations are skipped. Emit one note at the top of the audit: `degraded: milestone state unavailable on kanbanflow — milestone audit and date proposals omitted`. |
| jared-groom milestone coverage judgment check | `commands/jared-groom.md:16-17` | — | When MILESTONE_STATE is absent, skip the 'Milestone coverage' judgment check with note `degraded: milestone state unavailable on kanbanflow — milestone hygiene check omitted`. The Roadmap view guidance is GitHub-specific and should not appear. |
| jared-groom milestone-proximity ranking in stage.py (via jared-stage reference) | `commands/jared-stage.md:14-15` | — | When MILESTONE_STATE is absent, `stage.py` ranking falls back to 'Priority > age' (dropping the milestone-proximity dimension). The stub should note `degraded: milestone proximity unavailable on kanbanflow — staging ranked by Priority and age only`. |
| jared-init GitHub-specific bootstrap (field IDs, Roadmap view setup) | `commands/jared-init.md:23-38` | — | Step 3 milestone-related migration items (milestone coverage in sweep, Roadmap view setup guidance) are skipped on KanbanFlow. `sweep.py` milestone checks emit their own degradation notes. The init stub already conditionally handles GitHub vs KanbanFlow for the bootstrap itself, but does not yet tell Claude to suppress milestone-specific migration guidance on KanbanFlow. |
| milestones-and-roadmap reference — entire doc | `skills/jared/references/milestones-and-roadmap.md:L1` | yes | On KanbanFlow, milestones are not native (no open/close state, no due dates in the GitHub sense, no Roadmap view tied to the Projects v2 visual). Skip-with-note: 'MILESTONE_STATE unavailable on this backend — milestone creation, due dates, Roadmap view, and gh milestone PATCH commands do not apply.' |
| Roadmap view setup instructions | `skills/jared/references/milestones-and-roadmap.md:L68-84` | yes | KanbanFlow has no Projects v2 Roadmap view. Skip-with-note: 'MILESTONE_STATE unavailable — Roadmap view setup is GitHub Projects v2-specific.' |
| Milestone hygiene checklist in sweep — 'Roadmap view renders without gaps' | `skills/jared/references/milestones-and-roadmap.md:L86-92` | — | On KanbanFlow, there is no Roadmap view. Skip-with-note: 'MILESTONE_STATE unavailable — Roadmap view check does not apply.' |
| board-sweep.md §8 Milestone coverage | `skills/jared/references/board-sweep.md:L73-81` | — | On KanbanFlow, milestone open/close state and due dates are not available. Skip-with-note: 'MILESTONE_STATE unavailable — milestone date and state checks do not apply to this backend.' |
| structural-review.md Q3 — Milestones / Roadmap view | `skills/jared/references/structural-review.md:L33-45` | — | On KanbanFlow, milestone dates and Roadmap view are unavailable. Skip-with-note: 'MILESTONE_STATE unavailable — Q3 milestone/Roadmap checks do not apply.' |
| structural-review.md Q6 — Deliverables / milestone done-state | `skills/jared/references/structural-review.md:L62-76` | — | On KanbanFlow, milestone closed-state is unavailable. The deliverable sentence discipline is still useful as prose convention, but the done-state gating is not mechanically enforceable. Degrade to: omit milestone-state references; retain prose discipline as advisory. |
| SKILL.md frontmatter description — 'maintains milestones and the Roadmap view' | `skills/jared/SKILL.md:L3` | yes | On KanbanFlow, omit the Roadmap view claim or qualify: 'maintains milestones (GitHub only: Roadmap view).' Degrade to skip-with-note when milestone operations are invoked. |
| SKILL.md /jared-audit with --type milestones | `skills/jared/SKILL.md:L210` | yes | On KanbanFlow, milestone due dates unavailable. Skip-with-note: 'MILESTONE_STATE unavailable — --type milestones audit and date-based milestone triggers do not apply.' |

## NATIVE_DEPENDENCIES — 18 surface(s), 12 misleading

| Surface | File:line | Mislead | Degrade to |
|---|---|:--:|---|
| _cmd_blocked_by — success message | `skills/jared/scripts/jared:~734` | — | When NATIVE_DEPENDENCIES absent, change success message to note label-marker emulation: 'added blocked-by label marker #{dep} <- #{blocker} (label emulation — NATIVE_DEPENDENCIES not supported on this backend)'. |
| fetch_blocked_by_edges inside fetch_audit_window | `skills/jared/scripts/lib/board.py:~1926` | yes | Gate on NATIVE_DEPENDENCIES capability; when absent skip the blocked-by edges section of the audit window and emit a degradation note. |
| Board.fetch_open_issues_for_ties | `skills/jared/scripts/lib/board.py:~653` | yes | Gate on NATIVE_DEPENDENCIES; when absent either route through provider.list_open_items() without blocked_by edges (and note emulated edges must be parsed separately) or skip dependency analysis entirely with a note. |
| sweep check_native_dependencies | `skills/jared/scripts/sweep.py:427-441 (called at line 855-864)` | yes | Skip with a note: 'degraded: native-dependency hygiene unavailable on kanbanflow — NATIVE_DEPENDENCIES not supported' |
| dependency-graph fetch_all_native_dependencies | `skills/jared/scripts/dependency-graph.py:140-160 (used at line 394-395)` | — | Emit a degradation note when NATIVE_DEPENDENCIES is absent ('degraded: native blockedBy edges unavailable on kanbanflow — falling back to ## Depends on body-text parsing') instead of silently switching to body-text fallback. |
| stage fetch_items_for_stage (native blocked-by edges) | `skills/jared/scripts/stage.py:424-463 (edge fetch at line 441)` | yes | When NATIVE_DEPENDENCIES absent, emit a note: 'degraded: native blocked-by edges unavailable on kanbanflow — blocker detection relies on ## Blocked by body sections only'. The body-ref path still runs. |
| jared-reshape dependency graph via dependency-graph.py | `commands/jared-reshape.md:14` | — | When NATIVE_DEPENDENCIES is absent (only label-emulated edges available), note that the graph is built from emulated `blocked-by:<N>` label markers, not native edges: `degraded: native dependency edges unavailable on kanbanflow — graph built from label markers (emulated); cycle detection may be incomplete`. Do not silently skip — the emulated edges are still useful, but their limitations must be stated. |
| jared-groom dependency hygiene check | `commands/jared-groom.md:17` | — | Same as jared-reshape: note that the dependency check runs on emulated label-edges, not native ones. Emit `degraded: native dependency edges unavailable on kanbanflow — dependency hygiene based on emulated label markers`. Edge-level checks (closed-blocker detection, chain depth) may miss edges not captured by the label convention. |
| jared-file native GitHub edges instruction | `commands/jared-file.md:61-66` | yes | When NATIVE_DEPENDENCIES is absent, the stub should say `jared blocked-by` still works but via label emulation, not native edges: `degraded: native dependency edges unavailable on kanbanflow — blocked-by recorded as label marker`. The command itself is still callable; only the framing changes. |
| jared-audit open_dependents from fetch output (checklist Q6) | `commands/jared-audit.md:32-33` | yes | When NATIVE_DEPENDENCIES is absent, note in checklist question 6: `degraded: open_dependents unavailable on kanbanflow — check blocked-by label markers manually`. Do not show `open_dependents: 0` as if no dependents exist. |
| dependencies.md — native GitHub issue blockedBy as primary | `skills/jared/references/dependencies.md:L1-14` | yes | On KanbanFlow, native blockedBy edges do not exist (KanbanFlow uses a label-marker emulation). Skip-with-note: 'NATIVE_DEPENDENCIES unavailable — native blockedBy edges are GitHub-only; dependency tracking uses label-marker emulation on this backend.' |
| dependencies.md — jared blocked-by CLI subcommand | `skills/jared/references/dependencies.md:L8-14` | yes | On KanbanFlow, addBlockedBy GraphQL mutation is unavailable; label-marker emulation is used instead. Skip-with-note: 'NATIVE_DEPENDENCIES unavailable — jared blocked-by uses label emulation on this backend; edges do not render natively in the board.' |
| dependencies.md — gh api graphql blockedBy/blocking query | `skills/jared/references/dependencies.md:L27-47` | — | On KanbanFlow, blockedBy/blocking GraphQL fields are unavailable. Skip-with-note: 'NATIVE_DEPENDENCIES unavailable — gh api graphql dependency queries are GitHub-only.' |
| dependencies.md — dependency-graph.py uses native blockedBy | `skills/jared/references/dependencies.md:L61-93` | yes | On KanbanFlow, dependency-graph.py reads emulated label-marker edges, not native relations. Skip-with-note: 'NATIVE_DEPENDENCIES unavailable — dependency-graph.py reads label emulation; output is advisory only, not backed by native relation edges.' |
| board-sweep.md §9 Dependency hygiene — native blockedBy edges | `skills/jared/references/board-sweep.md:L83-93` | yes | On KanbanFlow, native blockedBy edges unavailable. Check reads emulated label-marker edges instead. Skip-with-note: 'NATIVE_DEPENDENCIES unavailable — native dependency hygiene check reads label emulation; output may be incomplete.' |
| structural-review.md Q4 — dependency graph via dependency-graph.py | `skills/jared/references/structural-review.md:L49-57` | yes | On KanbanFlow, dependency-graph.py works only on emulated edges. Skip-with-note: 'NATIVE_DEPENDENCIES unavailable — dependency graph is label-emulation only; cycles/inversions may not be detected if blockers are not labeled correctly.' |
| operations.md — native blockedBy GraphQL query | `skills/jared/references/operations.md:L122-130` | — | On KanbanFlow, this GraphQL query is inapplicable. Skip-with-note: 'NATIVE_DEPENDENCIES unavailable — gh api graphql blockedBy query is GitHub-only.' |
| jared-cli.md blocked-by subcommand | `skills/jared/references/jared-cli.md:L250-268` | yes | On KanbanFlow, GraphQL mutations are unavailable; label-marker emulation is used instead. Note should be added: 'NATIVE_DEPENDENCIES unavailable — on KanbanFlow, jared blocked-by uses label emulation; edges do not appear in a native panel.' |

## SUB_ISSUES — 2 surface(s), 0 misleading

| Surface | File:line | Mislead | Degrade to |
|---|---|:--:|---|
| SKILL.md — no SUB_ISSUES surface found | `skills/jared/SKILL.md:L1-348` | — | No degradation needed for this file. SUB_ISSUES capability not exercised in SKILL.md. |
| references/*.md — no SUB_ISSUES surface found across all 16 reference files | `skills/jared/references/dependencies.md:L1-126` | — | No degradation needed in the references region. SUB_ISSUES capability is not surfaced in skill/references prose. (Spec open question #4 — whether KanbanFlow subTasks satisfy SUB_ISSUES — does not require a finding here; no reference asserts it as universal.) |

## VELOCITY_TIMESTAMPS — 19 surface(s), 15 misleading

| Surface | File:line | Mislead | Degrade to |
|---|---|:--:|---|
| compute_velocity | `skills/jared/scripts/lib/board.py:~1751` | yes | Gate on VELOCITY_TIMESTAMPS capability before calling compute_velocity; return a sentinel/empty result with a degradation note when capability is absent. |
| fetch_audit_window — issues createdAt query | `skills/jared/scripts/lib/board.py:~1862` | yes | Skip timestamp-sorted issue query; degrade to provider.list_open_items() ordered by default, with a note that velocity-calibrated audit window is unavailable on this backend. |
| _cmd_next_session_prompt — recently closed section | `skills/jared/scripts/jared:~1086` | — | When VELOCITY_TIMESTAMPS absent, replace the section with a one-line note: 'degraded: recently-closed unavailable on kanbanflow — VELOCITY_TIMESTAMPS not supported'. |
| sweep check_stale_high_backlog | `skills/jared/scripts/sweep.py:347-371` | yes | Skip the check entirely with a note: 'degraded: stale-High-backlog unavailable on kanbanflow — VELOCITY_TIMESTAMPS not supported' |
| sweep check_in_progress_staleness | `skills/jared/scripts/sweep.py:374-396` | yes | Skip the check with a note: 'degraded: stalled-In-Progress unavailable on kanbanflow — VELOCITY_TIMESTAMPS not supported' |
| sweep check_blocked_status_hygiene (aging sub-check) | `skills/jared/scripts/sweep.py:399-424 (updatedAt branch at line 418-423)` | yes | Skip only the aging sub-check (the '## Blocked by' presence check can still run). Emit: 'degraded: blocked-aging check unavailable on kanbanflow — VELOCITY_TIMESTAMPS not supported' |
| sweep check_session_note_freshness | `skills/jared/scripts/sweep.py:542-582` | yes | Skip the check with a note: 'degraded: session-note freshness unavailable on kanbanflow — VELOCITY_TIMESTAMPS not supported' |
| stage days_in_backlog sort key | `skills/jared/scripts/stage.py:98-112 (used in rank_key at line 384)` | — | Tertiary sort key already saturates to 0 (least-aggressive sort) when absent; ranking degrades silently. Should emit a header note when VELOCITY_TIMESTAMPS absent: 'degraded: Backlog-age tiebreaker unavailable on kanbanflow — promotion order may differ' |
| jared-audit velocity-calibrated audit window | `commands/jared-audit.md:21-23` | yes | When VELOCITY_TIMESTAMPS is absent, fall back to the clamped floor (14 days) as the window — emit `degraded: velocity timestamps unavailable on kanbanflow — using 14-day static audit window`. The `velocity` block is absent from fetch output; proceed with null velocity for all calibration steps. |
| jared-audit milestone date formula (median_pr_duration_days) | `commands/jared-audit.md:75-84` | yes | When VELOCITY_TIMESTAMPS is absent, the date formula cannot be computed. Skip the formula; if a milestone reshape is still proposed (and MILESTONE_STATE is present on a different backend), use `today + 14` as the static floor and note `degraded: velocity timestamps unavailable — milestone date anchored to 14-day floor`. |
| jared-groom aging checks (7-day In Progress, 14-day High Backlog) | `commands/jared-groom.md:34-37` | yes | When VELOCITY_TIMESTAMPS is absent, collapse the Aging section to `degraded: timestamps unavailable on kanbanflow — aging checks omitted` rather than showing '0 days' or wrong values. Do not render the aging block. |
| jared status report aging flags | `commands/jared.md:15` | yes | When VELOCITY_TIMESTAMPS is absent, render the 'Worth a glance' section as `(degraded: timestamps unavailable on kanbanflow — aging data absent)` rather than showing incorrect day counts. Do not omit the heading (consistent shape across runs is valuable) but mark it degraded. |
| operations.md /jared-audit velocity block description | `skills/jared/references/operations.md:L149-152` | yes | On KanbanFlow, closed-at timestamps and transition times are unavailable. Velocity block cannot be computed. Skip-with-note: 'VELOCITY_TIMESTAMPS unavailable — velocity block and milestone-date calibration omitted.' An unguarded report would show zero/null velocity and propose artificially aggressive milestone dates. |
| SKILL.md Aging — 'In Progress items with no activity in 7 days' | `skills/jared/SKILL.md:L271` | yes | On KanbanFlow, transition/activity timestamps are unavailable. Aging checks cannot fire. Skip-with-note: 'VELOCITY_TIMESTAMPS unavailable — aging checks not available on this backend. All items appear fresh.' If not noted, the operator will believe the board is healthy when it may have stale items. |
| SKILL.md Cycle time — 'Captured passively at close' | `skills/jared/SKILL.md:L273` | — | On KanbanFlow, transition timestamps are unavailable; cycle time delta cannot be computed. Skip-with-note: 'VELOCITY_TIMESTAMPS unavailable — cycle time not captured on this backend.' |
| board-sweep.md §2 WIP — 'In Progress item with no activity in 7 days' | `skills/jared/references/board-sweep.md:L32-33` | yes | On KanbanFlow, activity timestamps unavailable. Check silently never fires; all In Progress items read as fresh. Skip-with-note: 'VELOCITY_TIMESTAMPS unavailable — stale In Progress check omitted; items may be abandoned without flagging.' |
| board-sweep.md §5 Aging — Backlog-High >14 days, any item >60 days | `skills/jared/references/board-sweep.md:L49-52` | yes | On KanbanFlow, timestamps unavailable. Aging checks silently never fire; old backlog items read as current. Skip-with-note: 'VELOCITY_TIMESTAMPS unavailable — aging checks omitted; backlog may contain undetected stale items.' |
| board-sweep.md §11 Session-note freshness — 'Session note from within last 3 working days' | `skills/jared/references/board-sweep.md:L109-115` | yes | On KanbanFlow, comment/activity timestamps unavailable. Session-note freshness check cannot fire. Skip-with-note: 'VELOCITY_TIMESTAMPS unavailable — session-note freshness check omitted.' |
| session-continuity.md — 'Recently closed (last 7 days)' in posture output | `skills/jared/references/session-continuity.md:L173` | — | On KanbanFlow, closed-at timestamps unavailable. 'Recently closed' section cannot be computed. Skip-with-note: 'VELOCITY_TIMESTAMPS unavailable — recently-closed section omitted from session posture.' |

