# skill-creator evaluation workspaces — heavy, per-machine, not versioned
*-workspace/

# Python
__pycache__/
*.py[cod]
*.pyo
.pytest_cache/
.mypy_cache/
.ruff_cache/

# OS crud
.DS_Store
Thumbs.db

# Editor leftovers
*.swp
*.swo
.vscode/
.idea/

# Test outputs that accidentally landed here
*.log
/tmp/

# Test config
tests/testbed.env

# Per-user Claude Code permissions (not versioned)
.claude/settings.local.json

# Claude Code per-machine runtime state (lock files, scheduled-task state)
.claude/scheduled_tasks.lock

# Per-user Claude Code project memory (private, not versioned)
CLAUDE.local.md

# uv lockfile — venv tooling is pip-based per CLAUDE.md; uv usage is incidental
uv.lock

# Local venv
.venv/

# Editable install metadata
*.egg-info/
